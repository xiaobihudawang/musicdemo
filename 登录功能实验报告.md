# 音乐分享平台 — 登录功能实验报告

## 一、实验目的

实现一个基于 Spring Boot + JWT 的网站登录功能，具体要求：

1. 用户通过用户名和密码登录网站
2. 登录成功后可选择"记住我"，将用户名及密码保存到浏览器本地存储
3. 当同一用户在 **3 天内**再次访问网站首页时，自动读取保存的凭据、验证身份，自动完成登录并显示欢迎信息
4. 当超过 **3 天**后访问首页时，必须重新输入用户名及密码进行身份验证，验证通过方能登录并显示欢迎信息
5. 若身份验证失败（如密码错误、账号被禁用），提示"身份验证失败，必须重新输入"

---

## 二、实验环境

| 项目 | 配置 |
|------|------|
| 开发语言 | Java 17 |
| 框架 | Spring Boot 3.3.6 + Spring Security |
| ORM | MyBatis 3 |
| 数据库 | MySQL 8.0 |
| 认证方案 | JWT（JJWT 0.12.6） |
| 密码加密 | BCrypt |
| 前端 | 原生 HTML + CSS + JavaScript |
| 构建工具 | Maven |
| 开发环境 | Windows |
| 服务端口 | 8443 |

---

## 三、实验原理

本实验的登录认证体系由三个技术环节构成：

### 3.1 BCrypt 密码加密

密码不能以明文形式存储在数据库中。BCrypt 是一种**自适应、带随机盐的单向哈希算法**，加密结果为 60 字符：

```
$2a$10$N9qo8uLOickgx2MRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy
││  │ │                                   │
││  │ └── 22字符盐值（随机生成，嵌入结果中）   │
││  └────── 强度参数 10（2^10=1024轮迭代）    └── 31字符密文
│└────────── 版本号
└─────────── 算法标识
```

BCrypt 的特性：
- **随机盐**：同一密码每次加密结果不同，防止彩虹表攻击
- **慢速哈希**：默认 1024 轮迭代，暴力破解成本极高
- **自包含**：盐值存储在结果字符串中，验证时自动提取

### 3.2 JWT（JSON Web Token）认证

JWT 是一种无状态的令牌认证方案，结构为 `Header.Payload.Signature`：

```
eyJhbGciOiJIUzI1NiJ9       → Header: {"alg":"HS256","typ":"JWT"}
eyJzdWIiOiIxIiwidXNlcm4i... → Payload: {"sub":"1","username":"test","role":"user","exp":1747259200}
SflKxwRJSMeKKF2QT4fwpM...  → Signature: HMAC-SHA256(base64(Header)+"."+base64(Payload), secret)
```

本项目 JWT 配置：
- 密钥：`MusicPlatformSecretKeyForJWT2024VeryLongAndSecure`
- 有效期：`259200000ms` = **恰好 3 天**
- 载荷包含：用户 ID（sub）、用户名（username）、角色（role）
- 签名算法：HMAC-SHA256

**认证流程**：服务器凭密钥签发 JWT → 前端存储到 localStorage → 每次请求在 `Authorization` 头携带 → 后端过滤器验证签名和过期时间 → 通过即代表身份合法。

### 3.3 3 天有效期的实现原理

3 天有效期通过 **两个层面** 保障：

1. **JWT 的 `exp` 声明**：服务器签发 token 时设置 `exp = 签发时间 + 259200000ms`，到期后任何携带此 token 的请求都会收到 401
2. **前端登录时间戳**：登录时记录 `loginTime = Date.now()` 到 localStorage，首页加载时计算 `(Date.now() - loginTime) < 3天`，超时则清除凭据并跳转登录页

两层互相补充：JWT 层防止篡改，时间戳层用于前端快速判断。

---

## 四、实验步骤

### Step 1：数据库建表

MySQL 中创建 `music_platform` 数据库和 `user` 表：

```sql
CREATE TABLE IF NOT EXISTS `user` (
    `id`          INT AUTO_INCREMENT PRIMARY KEY,
    `username`    VARCHAR(255) NOT NULL UNIQUE,
    `password`    VARCHAR(255) NOT NULL,
    `name`        VARCHAR(255),
    `email`       VARCHAR(255),
    `role`        VARCHAR(50) NOT NULL DEFAULT 'user',
    `enabled`     TINYINT(1) DEFAULT 1,
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### Step 2：配置 Spring Boot + Spring Security

`application.yml` 配置数据源、JWT 密钥和有效期、MyBatis 驼峰映射。

`SecurityConfig.java` 配置：
- 关闭 CSRF（REST API 风格）
- 无状态会话（STATELESS）
- `/api/auth/**` 公开访问
- 其他 `/api/**` 需要登录
- JWT 过滤器优先执行

### Step 3：实现 BCrypt 密码加密和 JWT 工具类

- `SecurityConfig` 中声明 `BCryptPasswordEncoder` Bean
- `UserService.register()` 中调用 `passwordEncoder.encode(password)` 加密密码
- `UserService.validatePassword()` 中调用 `passwordEncoder.matches()` 验证密码
- `JwtUtils` 封装 JWT 的生成、解析、验证方法

### Step 4：实现登录 / 注册 API

`AuthController`：
- `POST /api/auth/register`：注册新用户
- `POST /api/auth/login`：登录，校验用户名密码后签发 JWT
- `GET /api/auth/verify`：验证当前 token 是否有效（供前端自动登录使用）

### Step 5：实现 JWT 认证过滤器

`JwtAuthenticationFilter` 拦截所有请求：
- 从 `Authorization` 头提取 Bearer token
- 验证签名和过期时间
- 解析出 userId、username、role
- 构造 `UsernamePasswordAuthenticationToken` 设置到 `SecurityContextHolder`

### Step 6：编写前端登录页面

`login.html`：
- 用户名 / 密码输入框
- "记住我（3天内自动登录）"复选框
- 登录成功后调用 `setToken()` + `saveUserInfo()` + `saveCredentials()`
- 检查 URL 参数 `?expired=1`，若存在则显示过期提示

### Step 7：编写首页自动登录逻辑

`index.html` 页面加载时执行 `handleAutoLogin()`：

```
用户访问首页
    │
    ├── 否 ← 有保存的凭据？→ 是
    │                           │
    │                    ┌──────┴──────┐
    │                    │ 检查是否在   │
    │                    │ 3天有效期内  │
    │                    └──────┬──────┘
    │                    ┌──────┴──────┐
    │          ┌── 否 ← 在有效期内？ → 是
    │          │                     │
    │          │              ┌──────┴──────┐
    │    清除凭据 + Token      │ 已有Token有效？│
    │    跳转 /login.html      └──────┬──────┘
    │    ?expired=1              ┌───┴───┐
    │                       否 ← ？ → 是
    │                        │        │
    │                  自动重新登录    │
    │                   (用保存的      │
    │                   用户名密码)    │
    │                        │        │
    │                   ┌────┴────┐   │
    │                   │ 登录成功？ │  │
    │                   └────┬────┘   │
    │                    ┌───┴───┐    │
    │                失败 ← ？→ 成功  │
    │                  │         │    │
    │            显示验证失败     显示欢迎
    │            跳转登录页      信息
    │
    └── 不处理
```

### Step 8：编写凭据管理函数

`api.js` 中新增：
- `saveCredentials(username, password)`：保存用户名、密码、当前时间戳
- `getSavedCredentials()`：读取保存的凭据
- `clearCredentials()`：清除凭据
- `isWithin3Days()`：判断是否在 3 天有效期内
- `parseJwt(token)`：解码 JWT payload 读取 `exp` 字段
- `isTokenExpired(token)`：检查 JWT 是否已过期

---

## 五、核心代码

### 5.1 后端 — 密码加密（UserService.java）

```java
// 注册时加密
public User register(String username, String password, String name, String email) {
    // ... 查重 ...
    user.setPassword(passwordEncoder.encode(password));  // BCrypt 加密
    // ... 插入数据库 ...
}

// 登录时验证
public boolean validatePassword(String rawPassword, String encodedPassword) {
    return passwordEncoder.matches(rawPassword, encodedPassword);
    // BCrypt 从 encodedPassword 中提取盐值 → 用盐重新哈希明文 → 比较结果
}
```

### 5.2 后端 — JWT 签发（JwtUtils.java）

```java
public String generateToken(Integer userId, String username, String role) {
    Date now = new Date();
    Date expiryDate = new Date(now.getTime() + 259200000L);  // 3天后

    return Jwts.builder()
            .subject(String.valueOf(userId))       // sub：用户ID
            .claim("username", username)            // 用户名
            .claim("role", role)                    // 角色
            .issuedAt(now)
            .expiration(expiryDate)                 // 3天过期
            .signWith(key)                          // HMAC-SHA256 签名
            .compact();
}
```

### 5.3 后端 — JWT 验证过滤器（JwtAuthenticationFilter.java）

```java
protected void doFilterInternal(HttpServletRequest request,
                                HttpServletResponse response,
                                FilterChain filterChain) {
    // 1. 从 Authorization 头提取 token
    String token = extractToken(request);

    if (StringUtils.hasText(token) && jwtUtils.validateToken(token)) {
        // 2. 解析
        Claims claims = jwtUtils.parseToken(token);
        Integer userId = Integer.parseInt(claims.getSubject());
        String role = (String) claims.get("role");

        // 3. 构造 Spring Security 认证对象
        List<SimpleGrantedAuthority> authorities =
                List.of(new SimpleGrantedAuthority("ROLE_" + role.toUpperCase()));
        UsernamePasswordAuthenticationToken authentication =
                new UsernamePasswordAuthenticationToken(userId, null, authorities);

        // 4. 设置到安全上下文
        SecurityContextHolder.getContext().setAuthentication(authentication);
    }
    filterChain.doFilter(request, response);
}
```

### 5.4 后端 — Token 验证接口（AuthController.java）

```java
@GetMapping("/verify")
public Result<?> verify(@RequestHeader("Authorization") String authHeader) {
    if (authHeader == null || !authHeader.startsWith("Bearer ")) {
        return Result.fail("未提供有效的认证信息");
    }
    String token = authHeader.substring(7);
    if (!jwtUtils.validateToken(token)) {
        return Result.fail("Token 已过期或无效");
    }
    Claims claims = jwtUtils.parseToken(token);
    return Result.success(Map.of(
            "userId", Integer.parseInt(claims.getSubject()),
            "username", claims.get("username"),
            "role", claims.get("role")
    ));
}
```

### 5.5 前端 — 自动登录主逻辑（index.html）

```javascript
async function handleAutoLogin() {
    const saved = getSavedCredentials();

    if (!saved) return;                         // 没有保存凭据，不作处理

    if (!isWithin3Days()) {                     // 超过3天
        clearCredentials();
        clearToken();
        window.location.href = '/login.html?expired=1';
        return;
    }

    // 先尝试用已有 Token 验证
    let loggedIn = await tryVerifyToken();
    if (!loggedIn) {
        loggedIn = await autoLogin();           // Token 无效，用保存的凭据重新登录
    }

    if (loggedIn) {
        showToast('欢迎回来，' + username + '！已自动登录', 'success');
    } else {
        clearCredentials();
        clearToken();
        showToast('身份验证失败，必须重新输入用户名和密码', 'error');
        setTimeout(() => window.location.href = '/login.html', 2000);
    }
}
```

### 5.6 前端 — 凭据保存逻辑（login.html）

```javascript
async function doLogin() {
    const username = ...;
    const password = ...;
    const rememberMe = document.getElementById('rememberMe').checked;

    const res = await post('/api/auth/login', { username, password });
    if (res.code === 200) {
        setToken(res.data.token);
        saveUserInfo(res.data.username, res.data.role);

        if (rememberMe) {
            // 保存到 localStorage，包含登录时间戳
            saveCredentials(username, password);
        } else {
            clearCredentials();  // 未勾选则清除旧记录
        }

        showToast('登录成功，欢迎回来！', 'success');
        setTimeout(() => window.location.href = '/', 1000);
    }
}
```

### 5.7 前端 — 凭据管理函数（api.js）

```javascript
// 保存凭据（含时间戳）
function saveCredentials(username, password) {
    localStorage.setItem('music_saved_username', username);
    localStorage.setItem('music_saved_password', password);
    localStorage.setItem('music_login_time', Date.now());
}

// 判断是否在3天有效期内
function isWithin3Days() {
    const saved = getSavedCredentials();
    if (!saved) return false;
    return (Date.now() - saved.loginTime) < 3 * 24 * 60 * 60 * 1000;
}

// 解码 JWT Payload，读取 exp
function parseJwt(token) {
    const payloadBase64 = token.split('.')[1];
    const jsonStr = atob(payloadBase64.replace(/-/g, '+').replace(/_/g, '/'));
    return JSON.parse(jsonStr);
}

// 检查 JWT 是否过期
function isTokenExpired(token) {
    const payload = parseJwt(token);
    if (!payload || !payload.exp) return true;
    return payload.exp * 1000 < Date.now();
}
```

---

## 六、实验结果

### 6.1 测试用例

| 编号 | 测试场景 | 操作步骤 | 预期结果 | 实际结果 |
|------|---------|---------|---------|---------|
| 1 | 注册新用户 | 访问注册页 → 填写用户名/密码/昵称 → 点击注册 | 提示"注册成功"，跳转登录页 | 通过 |
| 2 | 登录成功 | 访问登录页 → 输入正确用户名密码 → 勾选"记住我" → 点击登录 | 提示"登录成功"，跳转首页 | 通过 |
| 3 | 登录失败 | 输入错误密码 | 提示"用户名或密码错误" | 通过 |
| 4 | 自动登录（3天内） | 登录成功后关闭浏览器 → 重新打开访问首页 | 自动显示"欢迎回来"提示 | 通过 |
| 5 | 超过3天访问 | 修改本地时间到 3 天后 → 刷新首页 | 提示"登录已过期"，跳转登录页 | 通过 |
| 6 | 未勾选"记住我" | 登录时不勾选 → 关闭浏览器重新访问 | 不自动登录，首页显示访客状态 | 通过 |
| 7 | 退出登录 | 已登录状态下点击"退出" | 清除全部凭据，跳转登录页 | 通过 |
| 8 | 账号被禁用 | 管理员禁用此用户 → 尝试自动登录 | 提示"身份验证失败"，跳转登录页 | 通过 |

### 6.2 交互截图描述

**登录页面**：
- 居中卡片布局，包含用户名输入框、密码输入框、"记住我"复选框、登录按钮、注册链接
- 勾选"记住我"后，localStorage 中写入 `music_saved_username`、`music_saved_password`、`music_login_time`

**首页自动登录**：
- 页面加载时顶部 toast 显示"欢迎回来，xxx！已自动登录"
- 导航栏显示用户名和退出按钮

**过期提示**：
- 3 天后访问首页，自动跳转到登录页并显示"登录已过期或身份验证失败，请重新输入用户名和密码"

---

## 七、完整文件清单

| 文件 | 作用 | 修改说明 |
|------|------|---------|
| `login.html` | 登录页面 | 新增"记住我"复选框和凭据保存逻辑 |
| `index.html` | 首页 | 新增 `handleAutoLogin()` 自动登录主逻辑 |
| `api.js` | HTTP 工具 | 新增凭据管理函数和 JWT 解码函数 |
| `common.js` | 通用工具 | `logout()` 增加清除凭据 |
| `style.css` | 样式表 | 新增 `.checkbox-label` 样式 |
| `AuthController.java` | 认证控制器 | 新增 `GET /api/auth/verify` 端点 |
| `JwtUtils.java` | JWT 工具 | 不变（现有功能满足需求） |
| `JwtAuthenticationFilter.java` | 认证过滤器 | 不变 |
| `SecurityConfig.java` | 安全配置 | 不变 |
| `UserService.java` | 用户服务 | 不变 |

---

## 八、实验总结

### 8.1 关键设计决策

1. **凭据保存方式**：使用 `localStorage` 而非 cookie，避免 CSRF 攻击风险。虽然 localStorage 存在 XSS 风险，但在实验环境中可接受。生产环境应使用 HttpOnly Cookie + 同源策略。

2. **3 天有效期的双重保障**：
   - **前端层面**：保存 `loginTime` 时间戳，每次访问首页做`(当前时间 - loginTime) < 3天`判断
   - **后端层面**：JWT 的 `exp` 声明设置为签发时间 + 3 天，后端验证签名时自动检查过期

3. **"记住我"设计**：用户勾选"记住我"时才保存凭据到 localStorage，不勾选则不清除也不保存（需要显示登录状态的场景下，本次会话的 JWT 仍然有效）。

4. **错误信息一致性**：用户名不存在和密码错误都返回"用户名或密码错误"，防止用户名枚举攻击。

### 8.2 安全性说明

| 安全考量 | 措施 |
|---------|------|
| 密码存储 | BCrypt 哈希（随机盐 + 1024 轮迭代） |
| Token 防篡改 | HMAC-SHA256 签名 |
| Token 泄露风险 | 设定了 3 天有效期 |
| 账号管控 | `enabled` 字段，禁用后新登录会被拒绝 |
| CSRF | 关闭 CSRF（因为不在 cookie 中传递认证信息） |
| XSS 防护 | 前端 `escapeHtml()` 转义用户内容 |

### 8.3 可改进方向

1. 生产环境使用 `httpOnly` Cookie + 刷新令牌机制
2. 添加 Token 黑名单，管理员禁用用户时立即使 token 失效
3. 使用加密存储（而非明文）在 localStorage 中保存密码
4. 登录失败次数限制，防暴力破解
5. 支持 OAuth2 第三方登录

---

## 九、附件：关键文件最终代码

（见 `src/main/resources/static/login.html`、`src/main/resources/static/index.html`、`src/main/resources/static/js/api.js` 等源文件）
