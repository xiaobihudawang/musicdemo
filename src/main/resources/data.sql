-- ================================================================
-- 数据初始化脚本 — data.sql
--
-- 本文件用于在应用启动时插入初始测试数据。
-- 由于 application.yml 中配置了 sql.init.mode=never，
-- 此文件会被 Spring Boot 忽略，不会自动执行。
--
-- 如需初始化数据，可修改为 sql.init.mode=always，
-- 或在数据库中手动执行以下 INSERT 语句。
--
-- 建议的初始测试数据：
--   1. 管理员账号：admin / admin123（角色为 'admin'）
--   2. 测试用户：testuser / 123456（角色为 'user'）
--   3. 若干首示例音乐和评论
-- ================================================================