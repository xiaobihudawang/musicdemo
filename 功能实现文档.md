# 音乐分享平台 — 功能实现文档

> 以下按「用户操作路径」逐功能展开，标注了**前端按钮 → 请求 → Controller → Service → Mapper → SQL**的完整链路，并解释每个方法的设计意图。

---

## 目录

1. [用户注册](#1-用户注册)
2. [用户登录](#2-用户登录)
3. [Token 校验（首页自动登录）](#3-token-校验首页自动登录)
4. [获取音乐列表（分页+搜索）](#4-获取音乐列表分页搜索)
5. [音乐详情](#5-音乐详情)
6. [音乐文件播放](#6-音乐文件播放)
7. [音乐上传](#7-音乐上传)
8. [AI 生成简介](#8-ai-生成简介)
9. [音乐下载](#9-音乐下载)
10. [点赞 / 取消点赞](#10-点赞--取消点赞)
11. [评论功能](#11-评论功能)
12. [排行榜](#12-排行榜)
13. [管理员功能](#13-管理员功能)
14. [JWT 认证过滤器](#14-jwt-认证过滤器)
15. [AI 聊天（预留功能）](#15-ai-聊天预留功能)
16. [前端核心模块](#16-前端核心模块)
17. [数据表设计](#17-数据表设计)
18. [项目配置要点](#18-项目配置要点)
19. [B站音频下载](#19-b站音频下载)

---

## 1. 用户注册

### 操作路径
`register.html` → 填写表单 → 点击"注册"按钮 (`doRegister()`)

### 请求链路
```
doRegister()                         → POST /api/auth/register
AuthController.register()            → UserService.register()
UserMapper.insert()                  → SQL: INSERT INTO user (...)
```

### 关键方法实现

**前端 `doRegister()`** (register.html:47)
- 收集 `username`, `password`, `name`, `email` 四个字段
- 前端校验：必填username+password，密码6~18位
- 调用 `post('/api/auth/register', { username, password, name, email })`
- 成功 → 跳转 `/login.html`；失败 → `showToast(res.message)`

**后端 `AuthController.register()`** (AuthController.java:69)
- 接收 `Map<String, String>` 参数体
- 调用 `UserService.register()`，成功返回 `Result.success({id, username})`
- 捕获 `RuntimeException`（如用户名已存在）→ `Result.fail(e.getMessage())`

**后端 `UserService.register()`** (UserService.java:28)
- 先用 `userMapper.findByUsername()` 检查用户名是否已存在，存在则抛异常
- 创建 `User` 对象，password 用 `BCryptPasswordEncoder.encode()` 加密后存入
- name 为空则默认使用 username; email 为空则 null
- 默认 role=`"user"`, enabled=`true`
- 调用 `userMapper.insert(user)` 持久化

**Mapper `insert`** (UserMapper.xml:31)
- `useGeneratedKeys=true` 回填自增ID
```sql
INSERT INTO user (username, password, name, email, role, enabled)
VALUES (#{username}, #{password}, #{name}, #{email}, #{role}, #{enabled})
```

---

## 2. 用户登录

### 操作路径
`login.html` → 输入用户名/密码 → 勾选"记住我" → 点击"登录"按钮 (`doLogin()`)

### 请求链路
```
doLogin()                            → POST /api/auth/login
AuthController.login()               → UserService.findByUsername() + validatePassword()
JwtUtils.generateToken()             → 生成JWT
```

### 关键方法实现

**前端 `doLogin()`** (login.html:55)
- 收集 username, password, rememberMe
- 前端校验非空
- `post('/api/auth/login', { username, password })`
- 成功 → `setToken(res.data.token, rememberMe)` + `saveUserInfo(username, role, rememberMe)` → 跳转首页
- 记住我=true 存 `localStorage`，否则存 `sessionStorage`

**后端 `AuthController.login()`** (AuthController.java:33)
1. `UserService.findByUsername()` 查用户，不存在 → "用户名或密码错误"
2. 检查 `user.getEnabled()`，false → "账号已被禁用"
3. `UserService.validatePassword()` 用 BCrypt 比对密码，不匹配 → "用户名或密码错误"
4. 全部通过 → `JwtUtils.generateToken(user.getId(), user.getUsername(), user.getRole())` 生成JWT
5. 返回 `Result.success({token, username, role})`

**JwtUtils.generateToken()** (JwtUtils.java:36)
- 使用 HMAC-SHA256 算法（`Keys.hmacShaKeyFor`）
- Payload 中放入 subject=`userId`，claims=`username`, `role`
- 过期时间从 `application.yml` 的 `jwt.expiration` 读取（默认 259200000ms = 3天）
- `signWith(key)` 签名后返回紧凑格式JWT字符串

---

## 3. Token 校验（首页自动登录）

### 操作路径
`index.html` 加载 → `handleAutoLogin()` 自动执行

### 请求链路
```
handleAutoLogin()                    → GET /api/auth/verify (Header: Bearer token)
AuthController.verify()              → JwtUtils.validateToken() + parseToken()
```

### 关键方法实现

**前端 `handleAutoLogin()`** (index.html:129)
1. 从 storage 取 token，不存在或 `isTokenExpired()` → 清token 跳登录页
2. 服务端校验 `GET /api/auth/verify`，code!=200 → 清token 跳登录页

**`isTokenExpired()`** (api.js:57)
- 解码 JWT payload（base64 → JSON）
- 比较 `payload.exp * 1000` 与当前时间戳

**后端 `AuthController.verify()`** (AuthController.java:59)
- 从 `Authorization` 头提取 Bearer token
- `jwtUtils.validateToken()` 验签和过期
- 返回 `{userId, username, role}`

---

## 4. 获取音乐列表（分页+搜索）

### 操作路径
`index.html` 加载 → `loadList(1, '')`；或输入关键词点击"搜索"按钮 → `search()`

### 请求链路
```
loadList() / search()                → GET /api/music/list?page=1&size=10&keyword=xxx
MusicController.list()               → MusicService.list() + count()
MusicMapper.findList() + countList() → SQL
```

### 关键方法实现

**前端 `search()`** (index.html:117)
- 重置 `currentPage=1`，获取 `#keyword` 输入框的值
- 调用 `loadList(1, keyword)`

**前端 `loadList()`** (index.html:51)
- 构造 URL: `/api/music/list?page=${page}&size=${pageSize}`，keyword 不为空则追加 `&keyword=...`
- `get(url)` 请求，code=200 后渲染列表 + 分页

**前端 `renderList()`** (index.html:66)
- 遍历 data.list，生成 `<li class="music-item">`，显示标题（可点击跳详情）、歌手、上传者、时间、❤️💬⬇️ 计数
- 使用 `escapeHtml()` 防止 XSS

**前端 `renderPagination()`** (index.html:89)
- 根据 total 计算总页数，生成"上一页/数字/下一页"按钮

**后端 `MusicController.list()`** (MusicController.java:54)
- `@RequestParam` 接收 page(默认1), size(默认10), keyword(可选)
- `musicService.list(page, size, keyword)` 查数据
- `musicService.count(keyword)` 查总数
- 返回 `Result.success({list, total, page, size})`

**`MusicService.list()`** (MusicService.java:45)
- offset = (page-1) * size
- 调用 `musicMapper.findList(offset, size, keyword)`

**`MusicMapper.findList`** (MusicMapper.xml:25)
- LEFT JOIN user 表获取 username
- WHERE 条件：keyword 非空时模糊匹配 title 或 artist
- ORDER BY create_time DESC + LIMIT offset, size

**`MusicMapper.countList`** (MusicMapper.xml:40)
- 相同的 WHERE 条件，SELECT COUNT(*)
- 用于前端分页计算总页数

---

## 5. 音乐详情

### 操作路径
首页/排行榜点击歌曲标题链接 → `detail.html?id=xxx` → `loadDetail()`

### 请求链路
```
loadDetail()                         → GET /api/music/{id}
MusicController.detail()             → MusicService.findById() + CommentService.listByMusicId()
MusicMapper.findById()               → SQL
CommentMapper.findByMusicId()        → SQL
```

### 关键方法实现

**`loadDetail()`** (detail.html:36)
- URL参数取 `musicId`
- `get('/api/music/' + musicId)` 获取详情+评论
- 成功则 `renderDetail(musicData, comments)`，同时 `isLogined()` 时调用 `checkLikeStatus()` 查询点赞状态

**`renderDetail()`** (detail.html:75)
- 渲染：标题、歌手、上传者、时间、简介、`<audio>` 播放器（src=`/api/music/${music.id}/stream`）
- 点赞按钮、下载按钮、统计数据
- 管理员额外显示"删除"按钮
- 评论区域：发表评论表单 + 评论列表（每个评论显示用户名+时间+内容，管理员有删除按钮）

**后端 `MusicController.detail()`** (MusicController.java:66)
- 查音乐，不存在 → `Result.fail(NOT_FOUND)`
- 查评论列表
- 返回 `Result.success({music, comments})`

**`MusicMapper.findById`** (MusicMapper.xml:51)
- LEFT JOIN user 获取 username

---

## 6. 音乐文件播放

### 操作路径
详情页的 `<audio>` 组件自动加载 `/api/music/{id}/stream`

### 请求链路
```
<audio src="/api/music/{id}/stream">  → GET /api/music/{id}/stream
MusicController.stream()               → 文件流输出
```

**`MusicController.stream()`** (MusicController.java:151)
- `void` 返回类型，直接操作 `HttpServletResponse` 输出流
- 根据 ID 查音乐，不存在 → 404
- 读取磁盘文件，根据扩展名设置 Content-Type（如 `audio/mpeg`）
- 8KB 缓冲区循环写入输出流

### 设计意图
- 不暴露服务器文件存储路径（旧方案用 `/api/music/file/{filePath}` 直接映射磁盘目录）
- 播放端点 `/api/music/{id}/stream` 公开可访问（SecurityConfig 白名单），与需登录的下载端点 `/api/music/{id}/download` 分离

---

## 7. 音乐上传

### 操作路径
导航栏"上传音乐" → `upload.html` → 填写表单 → 选择文件 → 点击"上传"按钮 (`doUpload()`)

### 请求链路
```
doUpload()                           → POST /api/music/upload (multipart/form-data)
MusicController.upload()             → MusicService.upload()
MusicMapper.insert()                 → SQL
文件写入磁盘                          → D:/workspace/music/{uuid}.{ext}
```

### 关键方法实现

**前端 `doUpload()`** (upload.html:97)
- 收集 title, artist, description, file
- 前端校验：title+artist 必填，文件必选
- 前端校验格式：仅 .mp3/.flac/.wav/.aac/.ogg/.m4a/.mp4
- 前端校验大小：≤50MB
- 构造 `FormData`，使用原生 `fetch('/api/music/upload', { method: 'POST', headers: { Authorization }, body: formData })`
- **为什么不直接用 `post()`**：因为 `post()` 自动设 `Content-Type: application/json` 并 JSON 序列化 body，而上传需要 `multipart/form-data`
- 成功 → 跳转 `/detail.html?id=xxx`

**后端 `MusicController.upload()`** (MusicController.java:79)
- `@RequestParam` 接收 MultipartFile + title + artist + description(可选)
- `getCurrentUserId()` 从 SecurityContext 取当前用户ID
- 调用 `musicService.upload(file, title, artist, description, userId)`
- 异常处理：RuntimeException → `Result.fail(e.getMessage())`; IOException → "文件上传失败"

**`MusicService.upload()`** (MusicService.java:66)
1. **格式校验**：取文件扩展名转小写，检查是否在 `ALLOWED_EXTENSIONS` 中（`.mp3/.flac/.wav/.aac/.ogg/.m4a/.mp4`），否则抛异常
2. **生成唯一文件名**：`UUID.randomUUID() + ext`，防止文件名冲突
3. **磁盘写入**：检查目录存在性，`file.transferTo(destFile)` Spring 内置方法
4. **数据库插入**：创建 Music 对象，title/artist/description/filePath(仅存uuid名)/fileSize/likeCount=0/commentCount=0/downloadCount=0/userId
5. 调用 `musicMapper.insert(music)`，`useGeneratedKeys` 回填自增ID

---

## 8. AI 生成简介

### 操作路径
`upload.html` → 填写标题和歌手 → 点击"🤖 AI 生成"按钮 (`generateDescription()`)

### 请求链路
```
generateDescription()                → POST /api/ai/description
AiController.generateDescription()   → AiService.generateDescription()
                                     → DeepSeek API (HTTP POST)
```

### 关键方法实现

**前端 `generateDescription()`** (upload.html:75)
- 校验 title 必填
- 按钮显示"🤖 生成中..."并禁用防重复点击
- `post('/api/ai/description', { title, artist })`
- 成功 → 将 `res.data.description` 填入 `#description` 文本框

**后端 `AiController.generateDescription()`** (AiController.java:22)
- 校验 title 非空
- `aiService.generateDescription(title, artist)`
- 返回 `Result.success({description})`

**`AiService.generateDescription()`** (AiService.java:45)
- 构造 prompt：要求 DeepSeek 根据标题+歌手写30~80字中文简介，直接返回文本不加前缀
- 如果歌曲信息是虚构的则发挥创意
- 调用 `callDeepSeek()` 与 DeepSeek API 通信

**`AiService.callDeepSeek()`** (AiService.java:128)
- 用 `RestTemplate` 向 `${apiUrl}/v1/chat/completions` 发 POST
- Header: `Authorization: Bearer ${apiKey}`, `Content-Type: application/json`
- Body: `{model: "deepseek-chat", messages: [...], temperature, max_tokens}`
- 解析 response 中的 `choices[0].message.content`
- 异常时返回友好提示："抱歉，AI 助手出小差了"

---

## 9. 音乐下载

### 操作路径
`detail.html` → 点击"⬇ 下载"按钮 → `downloadMusic(id)`

### 请求链路
```
downloadMusic()                      → GET /api/music/{id}/download (fetch blob)
MusicController.download()           → MusicService.download() + 文件流输出
DownloadRecordMapper.insert()        → SQL: INSERT INTO download_record
MusicMapper.updateDownloadCount()    → SQL: UPDATE music SET download_count+1
```

### 关键方法实现

**前端 `downloadMusic()`** (detail.html:204)
- `isLogined()` 校验未登录则提示
- 使用原生 `fetch('/api/music/${id}/download', { headers: { Authorization } })` 获得 blob
- 创建 `<a>` 标签模拟下载，文件名从 `musicData.title` 拼接
- 成功下载后释放 `URL.revokeObjectURL()`

**后端 `MusicController.download()`** (MusicController.java:98)
- `void` 返回类型，直接操作 `HttpServletResponse` 输出流（因为要流式传输文件）
- 先查音乐是否存在，不存在设404
- 通过 `getCurrentUserIdOrNull()` 获取当前用户（允许未登录下载，但不记录下载行为）
- 登录用户 → `musicService.download(id, userId)` 记录下载
- 读取磁盘文件，设置 Content-Type（根据扩展名映射）、Content-Disposition（附件下载）
- 8KB 缓冲区循环写入输出流

**`MusicService.download()`** (MusicService.java:91)
- 查音乐，不存在抛异常
- 创建 `DownloadRecord` 插入记录表
- `musicMapper.updateDownloadCount(musicId)` 计数+1
- 返回 Music 对象供 Controller 读取文件路径

**Content-Type 映射** (MusicController.java:36)
```
.mp3 → audio/mpeg, .flac → audio/flac, .wav → audio/wav,
.aac → audio/aac, .ogg → audio/ogg, .m4a → audio/mp4, .mp4 → audio/mp4
```

---

## 10. 点赞 / 取消点赞

### 操作路径
`detail.html` → 点击点赞按钮 (`toggleLike()`)

### 请求链路（先查状态 → 再切换）
```
页面加载: checkLikeStatus()            → GET /api/music/{id}/like/status
LikeController.likeStatus()           → LikeService.isLiked()

点击按钮: toggleLike()                  → POST /api/music/{id}/like
LikeController.toggleLike()           → LikeService.toggle()
LikeRecordMapper.findByUserAndMusic()  → 判断是否已赞
LikeRecordMapper.insert() / delete()   → 新增/删除记录
MusicMapper.updateLikeCount()          → SQL: UPDATE music SET like_count±1
```

### 关键方法实现

**前端 `checkLikeStatus()`** (detail.html:59)
- `get('/api/music/' + musicId + '/like/status')`
- 设置 `liked` 标志位，调用 `updateLikeButton()` 更新按钮样式

**前端 `toggleLike()`** (detail.html:147)
- 未登录提示登录
- `post('/api/music/' + musicId + '/like')`
- 成功后更新 liked 状态和按钮样式，显示 toast 提示
- 调用 `loadDetail()` 刷新页面数据（更新计数）

**后端 `LikeController.likeStatus()`** (LikeController.java:37)
- `getCurrentUserId()` 取当前用户
- `likeService.isLiked(userId, id)` 查询点赞记录是否存在
- 返回 `{liked: true/false}`

**后端 `LikeController.toggleLike()`** (LikeController.java:27)
- `likeService.toggle(userId, id)` 切换点赞状态
- 返回 `{liked: true/false}`

**`LikeService.toggle()`** (LikeService.java:29)
- **核心设计**：用 `findByUserAndMusic()` 判断当前状态
  - 已存在记录 → 执行 `delete()` 取消点赞 + `updateLikeCount(-1)`，返回 `liked=false`
  - 不存在记录 → 执行 `insert()` 新增点赞 + `updateLikeCount(1)`，返回 `liked=true`
- `like_record` 表有 `UNIQUE KEY uk_user_music(user_id, music_id)`，保证一用户对一歌曲只能有一条记录
- **为什么用 toggle 而不是分开 like/unlike**：减少前端状态管理复杂度，前端只需要一个按钮，后端决定操作方向，幂等性好

**`MusicMapper.updateLikeCount`** (MusicMapper.xml:70)
```sql
UPDATE music SET like_count = like_count + #{delta} WHERE id = #{id}
```
- 使用 `+ delta` 而非直接赋值，支持并发操作，且无需先查当前值

---

## 11. 评论功能

### 11.1 发表评论

### 操作路径
`detail.html` → 输入评论 → 点击"发表评论"按钮 (`submitComment()`)

### 请求链路
```
submitComment()                      → POST /api/music/{id}/comments
CommentController.add()              → CommentService.add()
CommentMapper.insert()               → SQL
MusicMapper.updateCommentCount()     → SQL: UPDATE music SET comment_count+1
```

**前端 `submitComment()`** (detail.html:164)
- 校验内容非空
- `post('/api/music/' + musicId + '/comments', { content })`
- 成功 → `loadDetail()` 刷新

**后端 `CommentController.add()`** (CommentController.java:38)
- `comment.setMusicId(id)` 和 `comment.setUserId(getCurrentUserId())` 由服务端设置
- 调用 `commentService.add(comment)`

**`CommentService.add()`** (CommentService.java:33)
- `@Transactional` 保证原子性
- `commentMapper.insert(comment)` 写入评论
- `musicMapper.updateCommentCount(musicId, +1)` 同步更新计数
- **为什么一起更新**：`music.comment_count` 是个冗余计数器，避免每次展示列表都 COUNT 一次评论表

### 11.2 评论列表查询

### 操作路径
`detail.html` 加载时自动获取

### 请求链路
内联在 `GET /api/music/{id}` 的 `detail()` 方法中 → `CommentService.listByMusicId(id)` → `CommentMapper.findByMusicId()`

**`CommentMapper.findByMusicId`** (CommentMapper.xml:19)
```sql
SELECT c.*, u.username FROM comment c
LEFT JOIN user u ON c.user_id = u.id
WHERE c.music_id = #{musicId}
ORDER BY c.create_time DESC
```

### 11.3 删除评论

### 操作路径
管理员在详情页评论列表点"删除"按钮 → `deleteComment(commentId)`

### 请求链路
```
deleteComment()                      → DELETE /api/comments/{id}
CommentController.delete()           → CommentService.delete()
CommentMapper.findById()             → 校验存在性
CommentMapper.deleteById()           → SQL
MusicMapper.updateCommentCount()     → SQL: UPDATE music SET comment_count-1
```

**前端 `deleteComment()`** (detail.html:180)
- `confirm()` 确认
- `del('/api/comments/' + commentId)`
- 成功 → `loadDetail()` 刷新

**后端 `CommentController.delete()`** (CommentController.java:48)
- `getCurrentUserId()` 和 `getCurrentUserRole()` 获取操作者信息
- 只有**评论者自己**或**管理员**可删除
- 权限不足 → `Result.fail(FORBIDDEN)`

---

## 12. 排行榜

### 操作路径
导航栏"排行榜" → `ranking.html` → 默认显示👍点赞排行，可切换⬇️下载/💬评论 Tab

### 请求链路
```
loadRanking()                        → GET /api/ranking/{type}
switchTab(type)                      → GET /api/ranking/{type}
RankingController.weeklyLikes()      → RankingService.getWeeklyLikeTop10()
  / weeklyDownloads() / weeklyComments()
MusicMapper.findWeeklyLikeTop10()    → SQL
  / findWeeklyDownloadTop10() / findWeeklyCommentTop10()
```

### 关键方法实现

**`loadRanking()`** (ranking.html:51)
- `get('/api/ranking/' + currentType)` type 可选 `likes`, `downloads`, `comments`
- 渲染排行列表，前三名添加 `.top3` 样式（金色序号）

**后端 `RankingController`** (RankingController.java)
- 三个公开端点（无需登录）：`GET /api/ranking/likes`, `/downloads`, `/comments`
- 各自调用 `RankingService` 对应方法

**`RankingService.getWeekRange()`** (RankingService.java:33)
- 计算本周一 00:00:00 ~ 本周日 23:59:59 的时间范围
- 使用 `LocalDate.now().with(DayOfWeek.MONDAY)` 获取本周一日期
- **设计意图**：固定以自然周为单位，周一~周日

**`MusicMapper.findWeeklyLikeTop10`** (MusicMapper.xml:85)
```sql
SELECT m.*, u.username FROM like_record lr
JOIN music m ON lr.music_id = m.id
JOIN user u ON m.user_id = u.id
WHERE lr.create_time BETWEEN #{start} AND #{end}
GROUP BY lr.music_id
ORDER BY COUNT(lr.id) DESC
LIMIT 10
```
- **设计意图**：根据 `like_record`, `download_record`, `comment` 表的创建时间过滤本周数据，按同一首歌的出现次数倒序排列取前10
- **不直接使用 music 表的 like_count 字段**：因为那个是累计总数，排行榜需要的是本周新增数据

---

## 13. 管理员功能

### 13.1 用户管理

### 操作路径
管理员导航栏"用户管理" → `admin/users.html` → 点击"启用/禁用"按钮

**加载用户列表**
```
loadUsers()                          → GET /api/admin/users (需要 ROLE_ADMIN)
AdminController.users()              → UserService.findAll()
UserMapper.findAll()                 → SQL: SELECT * FROM user
```

**切换用户状态**
```
toggleStatus(id, enabled)            → PUT /api/admin/users/{id}/status
AdminController.toggleUserStatus()   → UserService.toggleEnabled()
UserMapper.updateEnabled()           → SQL: UPDATE user SET enabled=#{enabled}
```

**`UserService.toggleEnabled()`** (UserService.java:57)
- `@Transactional`
- 直接调用 `userMapper.updateEnabled(id, enabled)`
- **设计意图**：不删除用户（保持数据完整性），仅禁用，被禁用户下次登录时会被拦截

### 13.2 音乐管理（管理员）

### 操作路径
管理员导航栏"音乐管理" → `admin/music.html` → 点击"删除"按钮

```
loadMusic()                          → GET /api/music/list?page=1&size=100
deleteMusic(id)                      → DELETE /api/admin/music/{id}
AdminController.deleteMusic()        → MusicService.delete()
MusicMapper.deleteById()             → SQL: DELETE FROM music WHERE id=#{id}
```

### 13.3 删除评论（管理员）

详情页管理员删除评论按钮 → `DELETE /api/comments/{id}` → 同 [11.3](#113-删除评论)

### 安全控制

**`SecurityConfig.filterChain()`** (SecurityConfig.java:28)
- `/api/auth/**` + GET `/api/music/list` + GET `/api/music/*` + GET `/api/music/*/stream` + GET `/api/ranking/**` → **公开**
- `/api/admin/**` → **需要 `ROLE_ADMIN` 角色**
- 其他 `/api/**` → **需要登录认证**
- CSRF 禁用，Session 无状态（STATELESS）
- `JwtAuthenticationFilter` 在 `UsernamePasswordAuthenticationFilter` 之前执行

---

## 14. JWT 认证过滤器

### 处理时机
每个请求进入后、到达 Controller 前

### 实现
**`JwtAuthenticationFilter.doFilterInternal()`** (JwtAuthenticationFilter.java:27)
1. 从请求头 `Authorization: Bearer xxx` 提取 token
2. `jwtUtils.validateToken(token)` 验签+过期
3. 解析 claims 获得 `userId`, `username`, `role`
4. `userMapper.findById(userId)` 查用户是否还存在且 `enabled=true`
   - **设计意图**：即使用户 token 未过期，如果管理员已禁用该用户，拒绝放行
5. 构建 `UsernamePasswordAuthenticationToken(userId, null, authorities)` 放入 SecurityContext
   - principal = userId（Integer），方便 Controller 取用
   - authorities = `ROLE_USER` 或 `ROLE_ADMIN`
6. 若 token 无效或用户不存在/被禁用，**不清除已有认证**，只是不设置新认证，由后续 Security 规则决定是否放行

---

## 15. AI 聊天（预留功能）

### 现状
`AiService` + `AiController` 已实现，但**前端无页面绑定** `/api/ai/chat` 端点。

### `AiService.chat()` 实现 (AiService.java:53)
- **上下文管理**：每个 userId 维护一个聊天历史列表 `chatHistories`（内存 Map），最多保留最近10轮对话
- **系统提示**：定义 AI 角色为「音小助」，若 `contextMusicId` 不为空则传入当前歌曲信息作为上下文
- **推荐检测**：AI 回复中若包含 `【搜索:xxx】` 格式，自动搜索数据库返回匹配歌曲列表
- 调用 DeepSeek API 获取回复，同时维护历史记录（含 user + assistant 消息）

---

## 16. 前端核心模块

### `api.js` — API 工具模块
- **Token 管理**：`getToken/setToken/clearToken` 操作 `localStorage`/`sessionStorage`
- **Token 解码**：`parseJwt()` 解码 payload 获取 `exp` 字段，用于前端判断过期
- **HTTP 封装**：`request()` 自动带 `Authorization` 头，遇到 401 自动清除登录态跳转登录页
- **HTTP 方法**：`get/post/put/del` 四个快捷方法
- **Toast 提示**：`showToast()` 创建浮动提示 DOM 元素，2.5 秒自动消失

### `common.js` — 通用工具模块
- **导航栏**：`renderNavbar()` 根据登录状态和角色动态生成导航菜单（首页/排行榜/上传音乐/用户管理/音乐管理/用户名/退出）
- **`DOMContentLoaded` 自动注入导航栏**
- **Loading 动画**：`fadeOutLoading()` 最少显示1.2秒后淡出

---

## 17. 数据表设计

| 表名 | 核心字段 | 用途 |
|------|---------|------|
| `user` | id, username, password(BCrypt), name, email, role, enabled | 用户认证授权 |
| `music` | id, title, artist, description, file_path, file_size, like_count, comment_count, download_count, user_id(FK) | 音乐元数据 + 冗余计数器 |
| `comment` | id, content, user_id(FK), music_id(FK) | 评论内容 |
| `like_record` | id, user_id(FK), music_id(FK), **UK(user_id,music_id)** | 点赞记录（唯一约束防重复） |
| `download_record` | id, user_id(FK), music_id(FK) | 下载记录 |

**冗余计数器设计**：`music` 表维护 `like_count/comment_count/download_count` 三个计数器，每次操作时 `±1` 更新，避免每次都 COUNT 关联表。但排行榜数据直接查询关联表的实时数据，保证本周统计的准确性。

---

## 18. 项目配置要点

| 配置项 | 值 | 作用 |
|--------|----|------|
| `server.port` | 8443 | 服务端口 |
| `spring.sql.init.mode` | never | 不自动执行 data.sql |
| `mybatis.map-underscore-to-camel-case` | true | `user_id` 自动映射到 `userId` |
| `jwt.secret` | MusicPlatformSecretKeyForJWT2024VeryLongAndSecure | JWT 签名密钥 |
| `jwt.expiration` | 259200000 (3天) | Token 有效期 |
| `music.file-path` | D:/workspace/music/ | 音乐文件存储根目录 |
| `spring.servlet.multipart.max-file-size` | 50MB | 上传文件大小限制 |

---

## 19. B站音频下载

### 功能概述
输入 B站 视频链接，Java 调用 Python 脚本获取音频流地址，然后**透传到浏览器**（不保存到服务器磁盘）。

### 操作路径
导航栏"B站下载" → `bilibili.html` → 粘贴 B站视频链接 → 点击"开始下载"按钮 (`doDownload()`)

### 请求链路
```
doDownload()                         → POST /api/bilibili/download (fetch + blob)
BilibiliController.download()        → BilibiliService.extractBvid() + getAudioInfo()
BilibiliService.getAudioInfo()       → ProcessBuilder 调用 python bilibili_demo.py
bilibili_demo.py                     → 获取音频 URL → 打印 TITLE / URL 到 stdout
BilibiliController                   → 用 HttpURLConnection 从 B站 拉取音频流
                                     → 写入 HttpServletResponse (Content-Disposition: attachment)
前端                                 → 接收 blob → 创建 <a> 触发浏览器下载 → showToast('下载成功')
```

### 关键方法实现

**前端 `doDownload()`** (bilibili.html:36)
- 输入框粘贴 B站视频完整链接（如 `https://www.bilibili.com/video/BV1xx...`）
- 按钮置为"下载中..."防重复点击
- `fetch('/api/bilibili/download', { method: 'POST', headers: { Authorization }, body: JSON })`
- 成功 → 从响应 `Content-Disposition` 解析文件名，用 `URL.createObjectURL(blob)` 触发浏览器下载
- 完成后 `showToast('下载成功', 'success')`，不显示多余下载链接或播放器

**后端 `BilibiliController.download()`** (BilibiliController.java:30)
- 返回 `void`，直接操作 `HttpServletResponse` 输出流
- 成功时设置 `Content-Disposition: attachment; filename*=UTF-8''{编码后的标题.m4a}`
- 用 `HttpURLConnection` 连接 B站音频 URL（带 Referer / User-Agent 头），读取并写入 response
- 出错时写 JSON 到 response（前端通过 `response.ok` 判断）

**`BilibiliService.extractBvid()`** (BilibiliService.java:47)
- 正则 `BV[a-zA-Z0-9]{10,12}` 从 URL 中提取 BV 号
- 匹配失败抛异常

**`BilibiliService.getAudioInfo()`** (BilibiliService.java:69)
- **核心设计**：Java 通过 `ProcessBuilder` 启动 Python 脚本获取音频流地址（不下载到磁盘）
- 拼装命令：`python bilibili_demo.py {bvid}`
- 读取 Python 标准输出，匹配 `TITLE:` 和 `URL:` 前缀行
- 返回 `AudioInfo(title, audioUrl, backupUrl)` 给 Controller

**Python 脚本 `bilibili_demo.py`** (项目根目录)
- 使用 `requests` + WBI 签名，调用 B站 API 获取视频信息
- 步骤：获取视频标题/cid → WBI 签名 → 获取 DASH 音频流 URL
- 输出 `TITLE:xxx` 和 `URL:xxx` 到标准输出（Java 读取）
- **不保存文件到磁盘**

### 数据流向
```
B站服务器
  ↓ 1. Python 脚本通过 B站 API 获取音频流 URL
  ↓ 2. Java (HttpURLConnection) 从 B站直拉音频流
  ↓ 3. Java 写入 HttpServletResponse（内存透传，不入磁盘）
浏览器 ← 4. 接收 blob 触发浏览器下载
```
