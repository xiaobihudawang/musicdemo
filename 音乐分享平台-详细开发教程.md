# 音乐分享平台 - 手把手开发教程

> 本教程面向 Java 初学者，每一步都会详细解释"为什么这样做"和"这段代码是什么意思"。
> 请按照顺序从头到尾敲代码，不要在中间跳过步骤。

---

## 技术栈一览

| 层面 | 技术 | 版本/说明 |
|------|------|-----------|
| Java | JDK | 17 |
| 框架 | Spring Boot | 3.3.6 |
| 数据库 | MySQL | 8.0 |
| ORM | MyBatis | 3.0.4 |
| 认证 | Spring Security + JWT | jjwt 0.12.6 |
| 前端 | 纯 HTML + CSS + JS | 无框架 |
| 构建工具 | Maven | |
| 端口 | 8443 | 前后端共用一个端口 |

## 最终效果

完成后的项目是一个可以在浏览器中访问的音乐分享网站，功能包括：
- 用户注册和登录
- 浏览和搜索音乐列表
- 上传 MP3 音乐文件
- 在线播放音乐
- 给音乐点赞（可取消）
- 发表评论
- 下载音乐
- 排行榜（本周点赞/下载/评论 Top10）
- 管理员管理用户和音乐

---

## 第一步：环境准备

### 1.1 确认 JDK 17 已安装

打开命令行（Win+R，输入 cmd 回车），执行：

```bash
java -version
```

应该看到 `17.x.x` 字样。如果不是，需要先安装 JDK 17。

### 1.2 确认 Maven 已安装

```bash
mvn -version
```

应该显示 Maven 版本信息。如果没有，需要安装 Maven（用 IDEA 自带的 Maven 也可以）。

### 1.3 确认 MySQL 已安装并运行

```bash
mysql -u root -p
```

输入你的 MySQL 密码。进入后创建数据库：

```sql
CREATE DATABASE music_platform DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

### 1.4 创建音乐文件存储目录

```bash
mkdir D:\workspace\music
```

这个目录用来存放用户上传的 MP3 文件。

---

## 第二步：创建项目目录结构

在 `White-Jotter-master` 目录下，创建 `music-platform` 项目文件夹。

### 2.1 创建所有需要的目录

在命令行中逐条执行（或者在 IDEA 中手动创建）：

```bash
# 进入工作目录
cd C:\Users\Administrator\IdeaProjects\spring\White-Jotter-master

# 创建项目根目录
mkdir music-platform

# 创建 Java 源码目录（注意：这是 Maven 的约定目录结构）
mkdir music-platform\src\main\java\com\gm\music\config
mkdir music-platform\src\main\java\com\gm\music\controller
mkdir music-platform\src\main\java\com\gm\music\service
mkdir music-platform\src\main\java\com\gm\music\mapper
mkdir music-platform\src\main\java\com\gm\music\entity
mkdir music-platform\src\main\java\com\gm\music\common

# 创建资源目录
mkdir music-platform\src\main\resources\mapper
mkdir music-platform\src\main\resources\static\admin
mkdir music-platform\src\main\resources\static\css
mkdir music-platform\src\main\resources\static\js

# 创建测试目录（Maven 约定，可以空着）
mkdir music-platform\src\test\java
```

**目录结构说明**：

```
music-platform/
├── pom.xml                          ← Maven 配置文件
└── src/
    ├── main/
    │   ├── java/com/gm/music/
    │   │   ├── MusicApplication.java      ← 程序入口
    │   │   ├── config/                    ← 配置类
    │   │   ├── controller/                ← 控制器（处理 HTTP 请求）
    │   │   ├── service/                   ← 业务逻辑层
    │   │   ├── mapper/                    ← 数据库操作接口
    │   │   ├── entity/                    ← 数据实体类
    │   │   └── common/                    ← 通用类（返回格式等）
    │   └── resources/
    │       ├── application.yml            ← Spring Boot 配置
    │       ├── schema.sql                 ← 建表 SQL
    │       ├── data.sql                   ← 初始数据
    │       ├── mapper/                    ← MyBatis XML 映射文件
    │       └── static/                    ← 前端 HTML/CSS/JS
    └── test/                              ← 测试代码（本教程暂不用）
```

---

## 第三步：编写 pom.xml

**文件路径**：`music-platform/pom.xml`

pom.xml 是 Maven 项目的核心配置文件，它告诉 Maven：
- 这个项目叫什么名字
- 依赖哪些第三方库
- 用什么 JDK 版本编译

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- parent：继承 Spring Boot 父工程，统一版本管理 -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.6</version>
        <relativePath/>
    </parent>

    <!-- 项目坐标（GAV） -->
    <groupId>com.gm</groupId>
    <artifactId>music-platform</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>
    <name>music-platform</name>
    <description>Music Sharing Platform</description>

    <!-- 属性定义，方便统一管理版本号 -->
    <properties>
        <java.version>17</java.version>
        <jjwt.version>0.12.6</jjwt.version>
    </properties>

    <!-- 依赖列表 -->
    <dependencies>
        <!-- spring-boot-starter-web：提供 Web 开发能力（Tomcat + Spring MVC） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- spring-boot-starter-security：提供安全认证能力 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- mybatis-spring-boot-starter：MyBatis 与 Spring Boot 整合 -->
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
            <version>3.0.4</version>
        </dependency>

        <!-- mysql-connector-j：MySQL 数据库驱动 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- JWT 三件套：用于生成和验证 JWT Token -->
        <!-- jjwt-api：JWT 的 API 接口 -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <!-- jjwt-impl：JWT 的实现 -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <!-- jjwt-jackson：JWT 的 JSON 处理（使用 Jackson） -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- spring-boot-starter-test：测试支持 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- 构建配置 -->
    <build>
        <plugins>
            <!-- Spring Boot Maven 插件：可以打包为可执行 JAR -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
```

**知识点解释**：
- `<scope>runtime</scope>`：运行时才需要，编译时不需要。比如数据库驱动，代码里不需要直接引用它的类。
- `spring-boot-starter-web` 是一个"起步依赖"，它自动包含了 Spring MVC、内嵌 Tomcat、Jackson 等 Web 开发所需的全部依赖。
- `${jjwt.version}` 是引用上面 `<properties>` 中定义的变量，方便统一修改版本。

---

## 第四步：编写程序入口类

**文件路径**：`music-platform/src/main/java/com/gm/music/MusicApplication.java`

```java
package com.gm.music;

import org.mybatis.spring.annotation.MapperScan;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Spring Boot 应用程序入口
 * @SpringBootApplication 是一个组合注解，包含了：
 *   - @Configuration：标记这是一个配置类
 *   - @EnableAutoConfiguration：启用自动配置
 *   - @ComponentScan：自动扫描当前包及子包中的组件
 * @MapperScan 告诉 MyBatis 去哪里找 Mapper 接口
 */
@SpringBootApplication
@MapperScan("com.gm.music.mapper")
public class MusicApplication {
    public static void main(String[] args) {
        SpringApplication.run(MusicApplication.class, args);
    }
}
```

**知识点**：
- Java 中包名 `com.gm.music` 对应目录结构 `/com/gm/music/`
- `SpringApplication.run()` 启动内嵌的 Tomcat 服务器，默认端口是 8080
- `@MapperScan` 是必须的，否则 MyBatis 找不到你的 Mapper 接口，启动会报错

---

## 第五步：编写 application.yml

**文件路径**：`music-platform/src/main/resources/application.yml`

```yaml
# 服务器端口设为 8443（区别于默认的 8080）
server:
  port: 8443

spring:
  # 数据库连接配置
  datasource:
    # 注意：数据库名 music_platform 要和第一步创建的一致
    url: jdbc:mysql://localhost:3306/music_platform?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    # 改成你自己的 MySQL 用户名
    username: root
    # 改成你自己的 MySQL 密码
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  # SQL 初始化：启动时自动执行 schema.sql 建表
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

  # 文件上传大小限制（最大 50MB）
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

# MyBatis 配置
mybatis:
  # XML 映射文件的位置
  mapper-locations: classpath:mapper/*.xml
  # 实体类别名包，在 XML 中可以直接用类名而不用全路径
  type-aliases-package: com.gm.music.entity
  configuration:
    # 数据库下划线自动转 Java 驼峰：create_time → createTime
    map-underscore-to-camel-case: true

# JWT 自定义配置
jwt:
  # JWT 签名密钥（生产环境要用更复杂的密钥）
  secret: MusicPlatformSecretKeyForJWT2024VeryLongAndSecure
  # Token 有效期，单位毫秒（7200000 = 2 小时）
  expiration: 7200000

# 自定义音乐文件存储路径
music:
  file-path: D:/workspace/music/
```

**知识点**：
- `classpath:` 指的是 `src/main/resources/` 目录
- `map-underscore-to-camel-case: true` 非常重要！比如数据库字段 `like_count` 会自动映射到 Java 属性 `likeCount`
- `sql.init.mode: always` 表示每次启动都执行 SQL 初始化脚本

---

## 第六步：编写数据库建表脚本

**文件路径**：`music-platform/src/main/resources/schema.sql`

这个文件在 Spring Boot 启动时自动执行，创建数据库表。

```sql
-- =====================================================
-- 用户表：存储注册用户信息
-- =====================================================
CREATE TABLE IF NOT EXISTS `user` (
    `id`          INT AUTO_INCREMENT PRIMARY KEY,       -- 主键，自增
    `username`    VARCHAR(255) NOT NULL UNIQUE,          -- 用户名，唯一
    `password`    VARCHAR(255) NOT NULL,                 -- BCrypt 加密后的密码
    `name`        VARCHAR(255),                          -- 昵称（可选）
    `email`       VARCHAR(255),                          -- 邮箱（可选）
    `role`        VARCHAR(50) NOT NULL DEFAULT 'user',   -- 角色：user 普通用户 / admin 管理员
    `enabled`     TINYINT(1) DEFAULT 1,                  -- 是否启用：1启用 0禁用
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP     -- 创建时间
);

-- =====================================================
-- 音乐表：存储上传的音乐信息
-- =====================================================
CREATE TABLE IF NOT EXISTS `music` (
    `id`             INT AUTO_INCREMENT PRIMARY KEY,
    `title`          VARCHAR(255) NOT NULL,               -- 歌曲标题
    `artist`         VARCHAR(255) NOT NULL,               -- 歌手名
    `description`    VARCHAR(2000),                       -- 简介（最长 2000 字）
    `file_path`      VARCHAR(500) NOT NULL,               -- 文件在硬盘上的路径/文件名
    `file_size`      BIGINT DEFAULT 0,                    -- 文件大小（字节）
    `like_count`     INT DEFAULT 0,                       -- 点赞数（冗余字段，方便查询）
    `comment_count`  INT DEFAULT 0,                       -- 评论数（冗余字段）
    `download_count` INT DEFAULT 0,                       -- 下载数（冗余字段）
    `user_id`        INT NOT NULL,                        -- 上传者 ID
    `create_time`    DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`user_id`) REFERENCES `user`(`id`)       -- 外键：关联用户表
);

-- =====================================================
-- 评论表
-- =====================================================
CREATE TABLE IF NOT EXISTS `comment` (
    `id`          INT AUTO_INCREMENT PRIMARY KEY,
    `content`     VARCHAR(2000) NOT NULL,                 -- 评论内容
    `user_id`     INT NOT NULL,                           -- 评论者 ID
    `music_id`    INT NOT NULL,                           -- 被评论的音乐 ID
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`user_id`) REFERENCES `user`(`id`),
    FOREIGN KEY (`music_id`) REFERENCES `music`(`id`)
);

-- =====================================================
-- 点赞记录表：记录谁给哪首歌点了赞
-- =====================================================
CREATE TABLE IF NOT EXISTS `like_record` (
    `id`          INT AUTO_INCREMENT PRIMARY KEY,
    `user_id`     INT NOT NULL,
    `music_id`    INT NOT NULL,
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    -- 唯一约束：同一个用户对同一首歌只能有一条记录
    -- 这就是 Toggle 点赞的核心：有记录=已赞，没记录=未赞
    UNIQUE KEY `uk_user_music` (`user_id`, `music_id`),
    FOREIGN KEY (`user_id`) REFERENCES `user`(`id`),
    FOREIGN KEY (`music_id`) REFERENCES `music`(`id`)
);

-- =====================================================
-- 下载记录表（不设唯一约束，每次下载都记录一条）
-- =====================================================
CREATE TABLE IF NOT EXISTS `download_record` (
    `id`          INT AUTO_INCREMENT PRIMARY KEY,
    `user_id`     INT NOT NULL,
    `music_id`    INT NOT NULL,
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`user_id`) REFERENCES `user`(`id`),
    FOREIGN KEY (`music_id`) REFERENCES `music`(`id`)
);
```

**SQL 知识点**：
- `IF NOT EXISTS`：表不存在时才创建，避免重复执行报错
- `AUTO_INCREMENT`：主键自动增长，插入时不需要指定
- `DEFAULT CURRENT_TIMESTAMP`：插入时自动填入当前时间
- `UNIQUE KEY`：唯一索引，`like_record` 表用它保证"一人一赞"
- `FOREIGN KEY`：外键约束，保证数据完整性

---

## 第七步：初始管理员账号

**不要用 `data.sql` 预置账号**，因为 BCrypt 每次都生成不同的密文，写死在 SQL 中不安全且可能不匹配。

我们用一个更合理的方式创建管理员账号：

**1. 不创建 `data.sql` 文件**（应用程序启动时只执行 `schema.sql` 建表）

**2. 启动项目后，通过注册页面创建第一个用户**

打开浏览器访问 `http://localhost:8443/register.html`，注册：
- 用户名：`admin`
- 密码：`123`

**3. 用 MySQL 客户端将第一个注册的用户提升为管理员：**

```sql
-- 连接到 MySQL
mysql -u root -p

-- 切换到 music_platform 数据库
USE music_platform;

-- 将 admin 用户设为管理员角色
UPDATE user SET role = 'admin' WHERE username = 'admin';

-- 确认修改成功
SELECT id, username, role FROM user;

-- 退出
EXIT;
```

之后 `admin / 123` 就是管理员账号了。普通用户通过注册页面注册后角色默认是 `user`。

**知识点**：
- 种子数据不适合写 BCrypt 密文（每次生成都不一样）
- 生产环境通常由运维先创建管理员账号
- `role` 字段在 `user` 表中，默认值 `'user'`，手动改成 `'admin'` 即可拥有管理员权限

---

## 第八步：创建实体类（Entity）

实体类对应数据库中的表，每个字段对应一个列。我们不用 Lombok，手动写 getter/setter。

### 8.1 User 实体

**文件路径**：`music-platform/src/main/java/com/gm/music/entity/User.java`

```java
package com.gm.music.entity;

import java.time.LocalDateTime;

/**
 * 用户实体：对应 user 表
 * 字段名使用驼峰命名，与数据库的下划线命名对应
 * （因为 application.yml 中配置了 map-underscore-to-camel-case: true）
 */
public class User {
    private Integer id;
    private String username;
    private String password;
    private String name;
    private String email;
    private String role;
    private Boolean enabled;
    private LocalDateTime createTime;

    // ========== Getter / Setter ==========
    // 在 IDEA 中可以使用 Alt+Insert → Getter and Setter 一键生成
    // 下面是手动写的版本：

    public Integer getId() { return id; }
    public void setId(Integer id) { this.id = id; }

    public String getUsername() { return username; }
    public void setUsername(String username) { this.username = username; }

    public String getPassword() { return password; }
    public void setPassword(String password) { this.password = password; }

    public String getName() { return name; }
    public void setName(String name) { this.name = name; }

    public String getEmail() { return email; }
    public void setEmail(String email) { this.email = email; }

    public String getRole() { return role; }
    public void setRole(String role) { this.role = role; }

    public Boolean getEnabled() { return enabled; }
    public void setEnabled(Boolean enabled) { this.enabled = enabled; }

    public LocalDateTime getCreateTime() { return createTime; }
    public void setCreateTime(LocalDateTime createTime) { this.createTime = createTime; }
}
```

### 8.2 Music 实体

**文件路径**：`music-platform/src/main/java/com/gm/music/entity/Music.java`

```java
package com.gm.music.entity;

import java.time.LocalDateTime;

/**
 * 音乐实体：对应 music 表
 */
public class Music {
    private Integer id;
    private String title;
    private String artist;
    private String description;
    private String filePath;
    private Long fileSize;
    private Integer likeCount;
    private Integer commentCount;
    private Integer downloadCount;
    private Integer userId;
    private LocalDateTime createTime;

    // 非数据库字段：上传者用户名（查询时关联查出）
    private String username;

    public Integer getId() { return id; }
    public void setId(Integer id) { this.id = id; }

    public String getTitle() { return title; }
    public void setTitle(String title) { this.title = title; }

    public String getArtist() { return artist; }
    public void setArtist(String artist) { this.artist = artist; }

    public String getDescription() { return description; }
    public void setDescription(String description) { this.description = description; }

    public String getFilePath() { return filePath; }
    public void setFilePath(String filePath) { this.filePath = filePath; }

    public Long getFileSize() { return fileSize; }
    public void setFileSize(Long fileSize) { this.fileSize = fileSize; }

    public Integer getLikeCount() { return likeCount; }
    public void setLikeCount(Integer likeCount) { this.likeCount = likeCount; }

    public Integer getCommentCount() { return commentCount; }
    public void setCommentCount(Integer commentCount) { this.commentCount = commentCount; }

    public Integer getDownloadCount() { return downloadCount; }
    public void setDownloadCount(Integer downloadCount) { this.downloadCount = downloadCount; }

    public Integer getUserId() { return userId; }
    public void setUserId(Integer userId) { this.userId = userId; }

    public LocalDateTime getCreateTime() { return createTime; }
    public void setCreateTime(LocalDateTime createTime) { this.createTime = createTime; }

    public String getUsername() { return username; }
    public void setUsername(String username) { this.username = username; }
}
```

### 8.3 Comment 实体

**文件路径**：`music-platform/src/main/java/com/gm/music/entity/Comment.java`

```java
package com.gm.music.entity;

import java.time.LocalDateTime;

/**
 * 评论实体：对应 comment 表
 */
public class Comment {
    private Integer id;
    private String content;
    private Integer userId;
    private Integer musicId;
    private LocalDateTime createTime;

    // 关联字段（非数据库列，查询时 JOIN 得到）
    private String username;

    public Integer getId() { return id; }
    public void setId(Integer id) { this.id = id; }

    public String getContent() { return content; }
    public void setContent(String content) { this.content = content; }

    public Integer getUserId() { return userId; }
    public void setUserId(Integer userId) { this.userId = userId; }

    public Integer getMusicId() { return musicId; }
    public void setMusicId(Integer musicId) { this.musicId = musicId; }

    public LocalDateTime getCreateTime() { return createTime; }
    public void setCreateTime(LocalDateTime createTime) { this.createTime = createTime; }

    public String getUsername() { return username; }
    public void setUsername(String username) { this.username = username; }
}
```

### 8.4 LikeRecord 实体

**文件路径**：`music-platform/src/main/java/com/gm/music/entity/LikeRecord.java`

```java
package com.gm.music.entity;

import java.time.LocalDateTime;

/**
 * 点赞记录实体：对应 like_record 表
 */
public class LikeRecord {
    private Integer id;
    private Integer userId;
    private Integer musicId;
    private LocalDateTime createTime;

    public Integer getId() { return id; }
    public void setId(Integer id) { this.id = id; }

    public Integer getUserId() { return userId; }
    public void setUserId(Integer userId) { this.userId = userId; }

    public Integer getMusicId() { return musicId; }
    public void setMusicId(Integer musicId) { this.musicId = musicId; }

    public LocalDateTime getCreateTime() { return createTime; }
    public void setCreateTime(LocalDateTime createTime) { this.createTime = createTime; }
}
```

### 8.5 DownloadRecord 实体

**文件路径**：`music-platform/src/main/java/com/gm/music/entity/DownloadRecord.java`

```java
package com.gm.music.entity;

import java.time.LocalDateTime;

/**
 * 下载记录实体：对应 download_record 表
 */
public class DownloadRecord {
    private Integer id;
    private Integer userId;
    private Integer musicId;
    private LocalDateTime createTime;

    public Integer getId() { return id; }
    public void setId(Integer id) { this.id = id; }

    public Integer getUserId() { return userId; }
    public void setUserId(Integer userId) { this.userId = userId; }

    public Integer getMusicId() { return musicId; }
    public void setMusicId(Integer musicId) { this.musicId = musicId; }

    public LocalDateTime getCreateTime() { return createTime; }
    public void setCreateTime(LocalDateTime createTime) { this.createTime = createTime; }
}
```

**小技巧**：在 IDEA 中，写完属性后按 `Alt + Insert`，选择 `Getter and Setter`，选中所有属性，IDEA 会自动生成所有 getter/setter 方法。如果使用 Lombok（`@Data` 注解），则连 getter/setter 都不用手写。

---

## 第九步：创建 MyBatis Mapper（数据访问层）

MyBatis 采用"接口 + XML"的方式操作数据库。接口定义方法签名，XML 写具体 SQL。

### 9.1 UserMapper

**文件路径**：`music-platform/src/main/java/com/gm/music/mapper/UserMapper.java`

```java
package com.gm.music.mapper;

import com.gm.music.entity.User;
import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;

import java.util.List;

/**
 * UserMapper 接口
 * 不需要实现类，MyBatis 会动态代理生成实现
 */
public interface UserMapper {

    /** 根据用户名查用户（登录时用） */
    User findByUsername(@Param("username") String username);

    /** 插入用户（注册时用） */
    int insert(User user);

    /** 查询所有用户（管理员用） */
    List<User> findAll();

    /** 更新用户启用/禁用状态 */
    int updateEnabled(@Param("id") Integer id, @Param("enabled") Boolean enabled);
}
```

**对应的 XML 文件**：`music-platform/src/main/resources/mapper/UserMapper.xml`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace 必须和接口全路径一致 -->
<mapper namespace="com.gm.music.mapper.UserMapper">

    <!-- 结果映射：告诉 MyBatis 如何把查询结果映射到 User 对象 -->
    <resultMap id="userMap" type="com.gm.music.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="role" column="role"/>
        <result property="enabled" column="enabled"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <!-- 根据用户名查询 -->
    <select id="findByUsername" resultMap="userMap">
        SELECT * FROM `user` WHERE username = #{username}
    </select>

    <!-- 插入用户，useGeneratedKeys 表示获取数据库自增的主键 -->
    <insert id="insert" parameterType="com.gm.music.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, name, email, role, enabled)
        VALUES (#{username}, #{password}, #{name}, #{email}, #{role}, #{enabled})
    </insert>

    <!-- 查询所有用户 -->
    <select id="findAll" resultMap="userMap">
        SELECT * FROM `user` ORDER BY create_time DESC
    </select>

    <!-- 更新启用状态 -->
    <update id="updateEnabled">
        UPDATE `user` SET enabled = #{enabled} WHERE id = #{id}
    </update>
</mapper>
```

**知识点**：
- `namespace` 必须和接口的全限定类名一致，否则 MyBatis 找不到
- `<resultMap>` 中的 `property` 是 Java 属性名，`column` 是数据库列名
- `#{}` 是 MyBatis 的占位符，会自动处理参数绑定和 SQL 注入防护
- `useGeneratedKeys="true"` + `keyProperty="id"` 可以让插入后自动回填主键

### 9.2 MusicMapper

**文件路径**：`music-platform/src/main/java/com/gm/music/mapper/MusicMapper.java`

```java
package com.gm.music.mapper;

import com.gm.music.entity.Music;
import org.apache.ibatis.annotations.Param;

import java.util.List;

public interface MusicMapper {

    /** 分页 + 搜索查询音乐列表 */
    List<Music> findList(@Param("offset") int offset,
                         @Param("size") int size,
                         @Param("keyword") String keyword);

    /** 统计总条数（分页用） */
    int countList(@Param("keyword") String keyword);

    /** 根据 ID 查询单个音乐 */
    Music findById(@Param("id") Integer id);

    /** 插入音乐 */
    int insert(Music music);

    /** 删除音乐 */
    int deleteById(@Param("id") Integer id);

    /** 更新点赞数 */
    int updateLikeCount(@Param("id") Integer id, @Param("delta") int delta);

    /** 更新评论数 */
    int updateCommentCount(@Param("id") Integer id, @Param("delta") int delta);

    /** 更新下载数 */
    int updateDownloadCount(@Param("id") Integer id);
}
```

**对应的 XML 文件**：`music-platform/src/main/resources/mapper/MusicMapper.xml`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gm.music.mapper.MusicMapper">

    <resultMap id="musicMap" type="com.gm.music.entity.Music">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="artist" column="artist"/>
        <result property="description" column="description"/>
        <result property="filePath" column="file_path"/>
        <result property="fileSize" column="file_size"/>
        <result property="likeCount" column="like_count"/>
        <result property="commentCount" column="comment_count"/>
        <result property="downloadCount" column="download_count"/>
        <result property="userId" column="user_id"/>
        <result property="createTime" column="create_time"/>
        <result property="username" column="username"/>
    </resultMap>

    <!-- 分页搜索：
         LEFT JOIN 关联 user 表获取上传者用户名
         LIKE 用于关键词模糊搜索（标题和歌手两个字段）
         LIMIT offset, size 实现分页
    -->
    <select id="findList" resultMap="musicMap">
        SELECT m.*, u.username
        FROM music m
        LEFT JOIN `user` u ON m.user_id = u.id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (m.title LIKE CONCAT('%', #{keyword}, '%')
                     OR m.artist LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY m.create_time DESC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 统计条件匹配的总记录数 -->
    <select id="countList" resultType="int">
        SELECT COUNT(*) FROM music
        <where>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%')
                     OR artist LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <select id="findById" resultMap="musicMap">
        SELECT m.*, u.username
        FROM music m
        LEFT JOIN `user` u ON m.user_id = u.id
        WHERE m.id = #{id}
    </select>

    <insert id="insert" parameterType="com.gm.music.entity.Music" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO music (title, artist, description, file_path, file_size, like_count, comment_count, download_count, user_id)
        VALUES (#{title}, #{artist}, #{description}, #{filePath}, #{fileSize}, #{likeCount}, #{commentCount}, #{downloadCount}, #{userId})
    </insert>

    <delete id="deleteById">
        DELETE FROM music WHERE id = #{id}
    </delete>

    <!-- 更新点赞数：delta 可以是 +1（点赞）或 -1（取消点赞） -->
    <update id="updateLikeCount">
        UPDATE music SET like_count = like_count + #{delta} WHERE id = #{id}
    </update>

    <update id="updateCommentCount">
        UPDATE music SET comment_count = comment_count + #{delta} WHERE id = #{id}
    </update>

    <update id="updateDownloadCount">
        UPDATE music SET download_count = download_count + 1 WHERE id = #{id}
    </update>
</mapper>
```

**知识点**：
- `<if test="...">` 是 MyBatis 的动态 SQL，条件成立时才拼接 SQL 片段
- `LIMIT #{offset}, #{size}`：MySQL 分页语法，offset 是起始位置，size 是条数
- `CONCAT('%', #{keyword}, '%')`：MySQL 字符串拼接，实现模糊搜索
- `like_count = like_count + #{delta}`：使用数据库原子更新，避免并发问题

### 9.3 CommentMapper

**文件路径**：`music-platform/src/main/java/com/gm/music/mapper/CommentMapper.java`

```java
package com.gm.music.mapper;

import com.gm.music.entity.Comment;
import org.apache.ibatis.annotations.Param;

import java.util.List;

public interface CommentMapper {

    /** 查询某首音乐的评论列表（按时间倒序） */
    List<Comment> findByMusicId(@Param("musicId") Integer musicId);

    /** 插入评论 */
    int insert(Comment comment);

    /** 根据 ID 查评论（用于权限验证） */
    Comment findById(@Param("id") Integer id);

    /** 删除评论 */
    int deleteById(@Param("id") Integer id);
}
```

**对应的 XML 文件**：`music-platform/src/main/resources/mapper/CommentMapper.xml`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gm.music.mapper.CommentMapper">

    <resultMap id="commentMap" type="com.gm.music.entity.Comment">
        <id property="id" column="id"/>
        <result property="content" column="content"/>
        <result property="userId" column="user_id"/>
        <result property="musicId" column="music_id"/>
        <result property="createTime" column="create_time"/>
        <result property="username" column="username"/>
    </resultMap>

    <select id="findByMusicId" resultMap="commentMap">
        SELECT c.*, u.username
        FROM comment c
        LEFT JOIN `user` u ON c.user_id = u.id
        WHERE c.music_id = #{musicId}
        ORDER BY c.create_time DESC
    </select>

    <select id="findById" resultMap="commentMap">
        SELECT c.*, u.username FROM comment c
        LEFT JOIN `user` u ON c.user_id = u.id
        WHERE c.id = #{id}
    </select>

    <insert id="insert" parameterType="com.gm.music.entity.Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (content, user_id, music_id) VALUES (#{content}, #{userId}, #{musicId})
    </insert>

    <delete id="deleteById">
        DELETE FROM comment WHERE id = #{id}
    </delete>
</mapper>
```

### 9.4 LikeRecordMapper

**文件路径**：`music-platform/src/main/java/com/gm/music/mapper/LikeRecordMapper.java`

```java
package com.gm.music.mapper;

import com.gm.music.entity.LikeRecord;
import org.apache.ibatis.annotations.Param;

public interface LikeRecordMapper {

    /** 插入点赞记录 */
    int insert(LikeRecord likeRecord);

    /** 删除点赞记录（取消点赞） */
    int delete(@Param("userId") Integer userId, @Param("musicId") Integer musicId);

    /** 检查是否已赞 */
    LikeRecord findByUserAndMusic(@Param("userId") Integer userId, @Param("musicId") Integer musicId);
}
```

**对应的 XML 文件**：`music-platform/src/main/resources/mapper/LikeRecordMapper.xml`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gm.music.mapper.LikeRecordMapper">

    <resultMap id="likeRecordMap" type="com.gm.music.entity.LikeRecord">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="musicId" column="music_id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.gm.music.entity.LikeRecord">
        INSERT INTO like_record (user_id, music_id) VALUES (#{userId}, #{musicId})
    </insert>

    <delete id="delete">
        DELETE FROM like_record WHERE user_id = #{userId} AND music_id = #{musicId}
    </delete>

    <select id="findByUserAndMusic" resultMap="likeRecordMap">
        SELECT * FROM like_record WHERE user_id = #{userId} AND music_id = #{musicId}
    </select>
</mapper>
```

### 9.5 DownloadRecordMapper

**文件路径**：`music-platform/src/main/java/com/gm/music/mapper/DownloadRecordMapper.java`

```java
package com.gm.music.mapper;

import com.gm.music.entity.DownloadRecord;
import org.apache.ibatis.annotations.Param;

public interface DownloadRecordMapper {

    /** 插入下载记录 */
    int insert(DownloadRecord downloadRecord);
}
```

**对应的 XML 文件**：`music-platform/src/main/resources/mapper/DownloadRecordMapper.xml`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gm.music.mapper.DownloadRecordMapper">

    <insert id="insert" parameterType="com.gm.music.entity.DownloadRecord">
        INSERT INTO download_record (user_id, music_id) VALUES (#{userId}, #{musicId})
    </insert>
</mapper>
```

---

## 第十步：创建通用响应类

所有 API 返回统一的 JSON 格式，方便前端处理。

### 10.1 ResultCode（状态码枚举）

**文件路径**：`music-platform/src/main/java/com/gm/music/common/ResultCode.java`

```java
package com.gm.music.common;

/**
 * 响应状态码
 * 200：成功
 * 400：请求错误
 * 401：未认证
 * 403：无权限
 * 500：服务器错误
 */
public enum ResultCode {
    SUCCESS(200, "操作成功"),
    FAIL(400, "操作失败"),
    UNAUTHORIZED(401, "请先登录"),
    FORBIDDEN(403, "权限不足"),
    NOT_FOUND(404, "资源不存在"),
    INTERNAL_ERROR(500, "服务器内部错误");

    private final int code;
    private final String message;

    ResultCode(int code, String message) {
        this.code = code;
        this.message = message;
    }

    public int getCode() { return code; }
    public String getMessage() { return message; }
}
```

### 10.2 Result（统一响应体）

**文件路径**：`music-platform/src/main/java/com/gm/music/common/Result.java`

```java
package com.gm.music.common;

/**
 * 统一 API 返回格式
 * 所有 Controller 的返回值都包装成这个对象
 *
 * 返回 JSON 样例如下：
 * {"code": 200, "message": "操作成功", "data": {...}}
 */
public class Result<T> {
    private int code;
    private String message;
    private T data;

    public Result() {}

    public Result(int code, String message, T data) {
        this.code = code;
        this.message = message;
        this.data = data;
    }

    // ---------- 静态工厂方法（推荐使用这些来创建 Result） ----------

    /** 成功（带数据） */
    public static <T> Result<T> success(T data) {
        return new Result<>(ResultCode.SUCCESS.getCode(), ResultCode.SUCCESS.getMessage(), data);
    }

    /** 成功（无数据） */
    public static <T> Result<T> success() {
        return new Result<>(ResultCode.SUCCESS.getCode(), ResultCode.SUCCESS.getMessage(), null);
    }

    /** 失败（自定义消息） */
    public static <T> Result<T> fail(String message) {
        return new Result<>(ResultCode.FAIL.getCode(), message, null);
    }

    /** 失败（使用预定义状态码） */
    public static <T> Result<T> fail(ResultCode code) {
        return new Result<>(code.getCode(), code.getMessage(), null);
    }

    /** 失败（自定义消息和状态码） */
    public static <T> Result<T> fail(int code, String message) {
        return new Result<>(code, message, null);
    }

    // ---------- Getter / Setter ----------

    public int getCode() { return code; }
    public void setCode(int code) { this.code = code; }

    public String getMessage() { return message; }
    public void setMessage(String message) { this.message = message; }

    public T getData() { return data; }
    public void setData(T data) { this.data = data; }
}
```

**知识点**：
- `<T>` 是 Java 泛型，`data` 字段可以是任何类型（List、对象、Map 等）
- 返回给前端的 `data` 字段会被 Spring MVC 自动序列化为 JSON
- 静态工厂方法让代码更可读：`Result.success(user)` vs `new Result(200, "成功", user)`

---

## 第十一步：JWT 认证层

JWT（JSON Web Token）是一种无状态认证方式：
1. 用户登录成功后，服务器生成一个 Token 返回给前端
2. 前端把 Token 存在 localStorage 中
3. 后续请求在 HTTP Header 中带上 `Authorization: Bearer <token>`
4. 服务器端过滤器验证 Token，提取用户信息

### 11.1 JwtUtils（JWT 工具类）

**文件路径**：`music-platform/src/main/java/com/gm/music/config/JwtUtils.java`

```java
package com.gm.music.config;

import io.jsonwebtoken.*;
import io.jsonwebtoken.security.Keys;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import javax.crypto.SecretKey;
import java.nio.charset.StandardCharsets;
import java.util.Date;
import java.util.Map;

/**
 * JWT 工具类
 * 负责：生成 Token、解析 Token、验证 Token
 */
@Component  // 交给 Spring 管理，可以用 @Value 注入配置
public class JwtUtils {

    private final SecretKey key;
    private final long expiration;

    /**
     * 构造器注入配置
     * @Value 从 application.yml 中读取 jwt.secret 和 jwt.expiration
     */
    public JwtUtils(@Value("${jwt.secret}") String secret,
                    @Value("${jwt.expiration}") long expiration) {
        // 用密钥字符串生成 HMAC-SHA 密钥
        this.key = Keys.hmacShaKeyFor(secret.getBytes(StandardCharsets.UTF_8));
        this.expiration = expiration;
    }

    /**
     * 生成 JWT Token
     * @param userId   用户 ID
     * @param username 用户名
     * @param role     角色（user / admin）
     */
    public String generateToken(Integer userId, String username, String role) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + expiration);

        return Jwts.builder()
                .subject(String.valueOf(userId))       // sub：主题，存用户 ID
                .claim("username", username)            // 自定义 claim：用户名
                .claim("role", role)                    // 自定义 claim：角色
                .issuedAt(now)                          // 签发时间
                .expiration(expiryDate)                 // 过期时间
                .signWith(key)                          // 签名
                .compact();                             // 生成字符串
    }

    /**
     * 从 Token 中解析出 Claims（存储的信息）
     * @throws JwtException 如果 Token 无效或过期
     */
    public Claims parseToken(String token) {
        return Jwts.parser()
                .verifyWith(key)
                .build()
                .parseSignedClaims(token)
                .getPayload();
    }

    /**
     * 验证 Token 是否有效
     */
    public boolean validateToken(String token) {
        try {
            parseToken(token);
            return true;
        } catch (JwtException | IllegalArgumentException e) {
            return false;
        }
    }

    /**
     * 从 Token 获取用户 ID
     */
    public Integer getUserId(String token) {
        String subject = parseToken(token).getSubject();
        return Integer.parseInt(subject);
    }
}
```

**知识点**：
- JWT 由三部分组成：Header.Payload.Signature，用 `.` 分隔
- `subject`（sub）是 JWT 标准字段，这里存用户 ID
- `claim` 是自定义字段，可以存任何信息
- `signWith(key)`：用密钥签名，防止 Token 被篡改
- 如果有人修改了 Token 内容，签名验证会失败，保证安全性

### 11.2 JwtAuthenticationFilter（JWT 认证过滤器）

**文件路径**：`music-platform/src/main/java/com/gm/music/config/JwtAuthenticationFilter.java`

```java
package com.gm.music.config;

import io.jsonwebtoken.Claims;
import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.core.authority.SimpleGrantedAuthority;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.security.web.authentication.WebAuthenticationDetailsSource;
import org.springframework.stereotype.Component;
import org.springframework.util.StringUtils;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;
import java.util.List;

/**
 * JWT 认证过滤器
 * 每个请求都会经过这个过滤器（继承 OncePerRequestFilter 确保每个请求只过滤一次）
 *
 * 工作流程：
 * 1. 从 HTTP Header 中取出 Authorization 的值
 * 2. 检查是否是 "Bearer " 开头
 * 3. 如果是，取出后面的 Token 部分
 * 4. 验证 Token，提取 user_id、username、role
 * 5. 设置到 Spring Security 的 SecurityContextHolder 中
 * 6. 后续的 Controller 可以通过 SecurityContextHolder 获取当前用户信息
 */
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {

    private final JwtUtils jwtUtils;

    public JwtAuthenticationFilter(JwtUtils jwtUtils) {
        this.jwtUtils = jwtUtils;
    }

    @Override
    protected void doFilterInternal(HttpServletRequest request,
                                     HttpServletResponse response,
                                     FilterChain filterChain)
            throws ServletException, IOException {

        // 1. 从请求头中提取 Token
        String token = extractToken(request);

        // 2. 如果 Token 存在且有效
        if (StringUtils.hasText(token) && jwtUtils.validateToken(token)) {
            // 3. 解析 Token
            Claims claims = jwtUtils.parseToken(token);
            Integer userId = Integer.parseInt(claims.getSubject());
            String username = (String) claims.get("username");
            String role = (String) claims.get("role");

            // 4. 构建认证对象
            // Spring Security 中，权限需要 ROLE_ 前缀
            List<SimpleGrantedAuthority> authorities =
                    List.of(new SimpleGrantedAuthority("ROLE_" + role.toUpperCase()));

            UsernamePasswordAuthenticationToken authentication =
                    new UsernamePasswordAuthenticationToken(userId, null, authorities);

            authentication.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));

            // 5. 设置到安全上下文中（后续代码可以通过 SecurityContextHolder 拿到）
            SecurityContextHolder.getContext().setAuthentication(authentication);
        }

        // 6. 继续执行后面的过滤器（很重要！不能忘记）
        filterChain.doFilter(request, response);
    }

    /**
     * 从请求头中提取 Bearer Token
     * Authorization 头的格式：Bearer eyJhbGciOiJIUzI1NiJ9...
     */
    private String extractToken(HttpServletRequest request) {
        String bearerToken = request.getHeader("Authorization");
        if (StringUtils.hasText(bearerToken) && bearerToken.startsWith("Bearer ")) {
            return bearerToken.substring(7); // 去掉 "Bearer " 前缀（7个字符）
        }
        return null;
    }
}
```

**知识点**：
- `OncePerRequestFilter`：Spring 提供的过滤器基类，保证在一次请求中只执行一次
- `SecurityContextHolder.getContext().setAuthentication()`：这是关键！设置了认证信息后，Spring Security 就知道"当前用户是谁"了
- `ROLE_` 前缀：Spring Security 约定，角色需要 `ROLE_` 前缀。`admin` 角色变成 `ROLE_ADMIN`
- `filterChain.doFilter()`：必须调用，把请求传给下一个过滤器，否则请求就会卡在这里

---

## 第十二步：Spring Security 配置

**文件路径**：`music-platform/src/main/java/com/gm/music/config/SecurityConfig.java`

Spring Security 配置类是整个安全体系的"交通警察"，它决定：
- 哪些 URL 需要认证，哪些不需要
- 使用什么方式认证（这里用 JWT）
- 密码怎么加密（BCrypt）

```java
package com.gm.music.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.http.HttpMethod;
import org.springframework.security.config.annotation.method.configuration.EnableMethodSecurity;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;

/**
 * Spring Security 配置类
 *
 * @Configuration      表示这是一个配置类
 * @EnableWebSecurity  启用 Spring Security 的 Web 安全功能
 */
@Configuration
@EnableWebSecurity
@EnableMethodSecurity  // 启用方法级别的权限控制（@PreAuthorize 等）
public class SecurityConfig {

    private final JwtAuthenticationFilter jwtAuthenticationFilter;

    public SecurityConfig(JwtAuthenticationFilter jwtAuthenticationFilter) {
        this.jwtAuthenticationFilter = jwtAuthenticationFilter;
    }

    /**
     * 密码编码器 Bean
     * BCryptPasswordEncoder 是一种安全的单向哈希算法
     * 相同的明文每次加密结果都不同（自带随机盐）
     */
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }

    /**
     * 安全过滤器链（核心配置）
     *
     * 请求处理流程：
     *   请求 → JwtAuthenticationFilter → HttpSecurity 规则匹配 → Controller
     *
     * URL 权限规则设计思路：
     *   游客（未登录）可以：浏览音乐列表、看详情、看排行榜、登录、注册、下载文件
     *   登录用户（user/admin）可以：上传音乐、评论、点赞、下载
     *   管理员（admin）可以：用户管理、删除音乐、删除评论
     */
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            // 禁用 CSRF（因为我们是 RESTful API，用 Token 认证，不需要 CSRF 保护）
            .csrf(csrf -> csrf.disable())

            // 无状态会话（不使用 HttpSession，完全依赖 JWT）
            .sessionManagement(session ->
                session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))

            // URL 权限配置
            .authorizeHttpRequests(auth -> auth
                // ===== 公开接口（游客可访问） =====
                .requestMatchers("/api/auth/**").permitAll()           // 登录、注册
                .requestMatchers(HttpMethod.GET, "/api/music/list").permitAll()    // 音乐列表
                .requestMatchers(HttpMethod.GET, "/api/music/{id}").permitAll()    // 音乐详情
                .requestMatchers(HttpMethod.GET, "/api/music/{id}/comments").permitAll() // 评论列表
                .requestMatchers(HttpMethod.GET, "/api/music/{id}/download").permitAll() // 下载
                .requestMatchers("/api/music/file/**").permitAll()     // 静态文件
                .requestMatchers("/api/ranking/**").permitAll()        // 排行榜

                // ===== 管理员专用接口 =====
                .requestMatchers("/api/admin/**").hasRole("ADMIN")

                // ===== 其他所有 /api/** 接口需要登录 =====
                .requestMatchers("/api/**").authenticated()

                // ===== 静态资源（前端页面）允许所有 =====
                .anyRequest().permitAll()
            )

            // 把 JWT 过滤器加到 UsernamePasswordAuthenticationFilter 之前
            // 这样 JWT 过滤器会先执行，解析出用户信息后设置认证信息
            .addFilterBefore(jwtAuthenticationFilter,
                           UsernamePasswordAuthenticationFilter.class);

        return http.build();
    }
}
```

**知识点**：
- `requestMatchers("/api/auth/**").permitAll()`：匹配以 `/api/auth/` 开头的所有路径，允许任何人访问
- `hasRole("ADMIN")`：需要 `ROLE_ADMIN` 权限。注意 Spring Security 会自动加 `ROLE_` 前缀，所以判断的是 `ROLE_ADMIN`
- `.anyRequest().permitAll()`：其他所有请求（比如访问静态 HTML 页面）全部放行
- `addFilterBefore(jwtFilter, UsernamePasswordAuthenticationFilter.class)`：把我们的 JWT 过滤器插入到认证过滤链中
- `.sessionCreationPolicy(SessionCreationPolicy.STATELESS)`：不用 Session，每个请求都独立认证

---

## 第十三步：Web 配置（静态资源映射）

**文件路径**：`music-platform/src/main/java/com/gm/music/config/WebConfig.java`

让 URL `/api/music/file/xxx.mp3` 映射到硬盘目录 `D:/workspace/music/xxx.mp3`。

```java
package com.gm.music.config;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.ResourceHandlerRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

/**
 * Web MVC 配置
 * 把虚拟 URL 路径映射到实际的文件目录
 */
@Configuration
public class WebConfig implements WebMvcConfigurer {

    @Value("${music.file-path}")
    private String filePath;

    /**
     * 添加资源处理器
     * 当浏览器访问 /api/music/file/xxx.mp3 时
     * 实际读取 D:/workspace/music/xxx.mp3 这个文件
     */
    @Override
    public void addResourceHandlers(ResourceHandlerRegistry registry) {
        registry.addResourceHandler("/api/music/file/**")
                .addResourceLocations("file:" + filePath);
    }
}
```

**知识点**：
- `addResourceHandler("/api/music/file/**")`：匹配 URL 路径 `/api/music/file/` 后跟任意内容
- `addResourceLocations("file:" + filePath)`：`file:` 前缀表示这是本地文件系统路径（不是 classpath）
- 比如：`/api/music/file/abc.mp3` → 实际读取 `D:/workspace/music/abc.mp3`

---

## 第十四步：Service 层（业务逻辑）

### 14.1 UserService

**文件路径**：`music-platform/src/main/java/com/gm/music/service/UserService.java`

```java
package com.gm.music.service;

import com.gm.music.entity.User;
import com.gm.music.mapper.UserMapper;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

/**
 * 用户服务
 * @Service 表示这是 Spring 管理的业务层 Bean
 */
@Service
public class UserService {

    private final UserMapper userMapper;
    private final PasswordEncoder passwordEncoder;

    /**
     * 构造器注入（推荐方式，比 @Autowired 字段注入更好）
     * Spring 会自动把 UserMapper 和 PasswordEncoder 传入
     */
    public UserService(UserMapper userMapper, PasswordEncoder passwordEncoder) {
        this.userMapper = userMapper;
        this.passwordEncoder = passwordEncoder;
    }

    /** 根据用户名查用户 */
    public User findByUsername(String username) {
        return userMapper.findByUsername(username);
    }

    /** 注册：密码需要 BCrypt 加密后才能存入数据库 */
    @Transactional
    public User register(String username, String password, String name, String email) {
        // 检查用户名是否已存在
        if (userMapper.findByUsername(username) != null) {
            throw new RuntimeException("用户名已存在");
        }

        User user = new User();
        user.setUsername(username);
        // BCrypt 加密：相同的明文每次生成的密文都不同
        user.setPassword(passwordEncoder.encode(password));
        user.setName(name != null ? name : username);  // 如果没填昵称就用用户名
        user.setEmail(email);
        user.setEnabled(true);
        user.setRole("user");  // 注册的用户默认是普通用户

        userMapper.insert(user);
        return user;
    }

    /** 验证密码 */
    public boolean validatePassword(String rawPassword, String encodedPassword) {
        return passwordEncoder.matches(rawPassword, encodedPassword);
    }

    /** 查询所有用户（管理员用） */
    public List<User> findAll() {
        return userMapper.findAll();
    }

    /** 切换用户启用/禁用状态 */
    @Transactional
    public void toggleEnabled(Integer id, Boolean enabled) {
        userMapper.updateEnabled(id, enabled);
    }
}
```

**知识点**：
- `@Transactional`：声明事务。如果方法中多次数据库操作，任一失败则全部回滚
- `passwordEncoder.encode()`：BCrypt 加密，同一个密码每次加密结果不同（自带随机盐）
- `passwordEncoder.matches(raw, encoded)`：验证密码，将明文与密文比对
- 构造器注入是 Spring 推荐的依赖注入方式，比 `@Autowired` 字段注入更易测试

### 14.2 MusicService

**文件路径**：`music-platform/src/main/java/com/gm/music/service/MusicService.java`

```java
package com.gm.music.service;

import com.gm.music.entity.Music;
import com.gm.music.mapper.DownloadRecordMapper;
import com.gm.music.mapper.MusicMapper;
import com.gm.music.entity.DownloadRecord;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.web.multipart.MultipartFile;

import java.io.File;
import java.io.IOException;
import java.util.List;
import java.util.UUID;

@Service
public class MusicService {

    private final MusicMapper musicMapper;
    private final DownloadRecordMapper downloadRecordMapper;

    @Value("${music.file-path}")
    private String filePath;

    public MusicService(MusicMapper musicMapper, DownloadRecordMapper downloadRecordMapper) {
        this.musicMapper = musicMapper;
        this.downloadRecordMapper = downloadRecordMapper;
    }

    /** 分页搜索音乐列表 */
    public List<Music> list(int page, int size, String keyword) {
        int offset = (page - 1) * size;  // 计算起始位置
        return musicMapper.findList(offset, size, keyword);
    }

    /** 统计符合条件的音乐总数 */
    public int count(String keyword) {
        return musicMapper.countList(keyword);
    }

    /** 根据 ID 查音乐详情 */
    public Music findById(Integer id) {
        return musicMapper.findById(id);
    }

    /**
     * 上传音乐
     * @param file 上传的文件
     * @param title 歌曲标题
     * @param artist 歌手
     * @param description 简介
     * @param userId 上传者 ID
     */
    @Transactional
    public Music upload(MultipartFile file, String title, String artist,
                        String description, Integer userId) throws IOException {
        // 1. 检查文件类型
        String originalFilename = file.getOriginalFilename();
        if (originalFilename == null || !originalFilename.toLowerCase().endsWith(".mp3")) {
            throw new RuntimeException("仅支持 MP3 格式");
        }

        // 2. 生成随机文件名（防止重复）
        // UUID 是全球唯一的随机字符串
        String newFilename = UUID.randomUUID().toString() + ".mp3";

        // 3. 创建存储目录（如果不存在）
        File dir = new File(filePath);
        if (!dir.exists()) {
            dir.mkdirs();
        }

        // 4. 保存文件到硬盘
        File destFile = new File(filePath + newFilename);
        file.transferTo(destFile);

        // 5. 保存音乐信息到数据库
        Music music = new Music();
        music.setTitle(title);
        music.setArtist(artist);
        music.setDescription(description);
        music.setFilePath(newFilename);  // 只存文件名，不存完整路径
        music.setFileSize(file.getSize());
        music.setLikeCount(0);
        music.setCommentCount(0);
        music.setDownloadCount(0);
        music.setUserId(userId);

        musicMapper.insert(music);
        return music;
    }

    /** 删除音乐（不删文件，只删数据库记录） */
    @Transactional
    public void delete(Integer id) {
        musicMapper.deleteById(id);
    }

    /** 下载音乐（记录下载记录 + 更新计数） */
    @Transactional
    public Music download(Integer musicId, Integer userId) {
        Music music = musicMapper.findById(musicId);
        if (music == null) {
            throw new RuntimeException("音乐不存在");
        }

        // 记录下载
        DownloadRecord record = new DownloadRecord();
        record.setUserId(userId);
        record.setMusicId(musicId);
        downloadRecordMapper.insert(record);

        // 更新下载计数
        musicMapper.updateDownloadCount(musicId);

        return music;
    }
}
```

**知识点**：
- `UUID.randomUUID().toString()`：生成类似 `a1b2c3d4-e5f6-...` 的字符串，保证文件名不冲突
- `file.transferTo(destFile)`：Spring 提供的文件写入方法
- `@Value("${music.file-path}")`：读取 application.yml 中的配置
- `int offset = (page - 1) * size`：前端传第 1 页，后端算 offset=0；第 2 页 offset=10（假如每页 10 条）

### 14.3 CommentService

**文件路径**：`music-platform/src/main/java/com/gm/music/service/CommentService.java`

```java
package com.gm.music.service;

import com.gm.music.entity.Comment;
import com.gm.music.mapper.CommentMapper;
import com.gm.music.mapper.MusicMapper;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

@Service
public class CommentService {

    private final CommentMapper commentMapper;
    private final MusicMapper musicMapper;

    public CommentService(CommentMapper commentMapper, MusicMapper musicMapper) {
        this.commentMapper = commentMapper;
        this.musicMapper = musicMapper;
    }

    /** 获取某首音乐的评论列表 */
    public List<Comment> listByMusicId(Integer musicId) {
        return commentMapper.findByMusicId(musicId);
    }

    /** 发表评论（同时更新音乐的 comment_count） */
    @Transactional
    public Comment add(Comment comment) {
        commentMapper.insert(comment);
        // 更新计数（delta = +1 表示加一条）
        musicMapper.updateCommentCount(comment.getMusicId(), 1);
        return comment;
    }

    /** 根据 ID 查评论 */
    public Comment findById(Integer id) {
        return commentMapper.findById(id);
    }

    /** 删除评论（同时更新音乐的 comment_count） */
    @Transactional
    public void delete(Integer id, Integer musicId) {
        commentMapper.deleteById(id);
        // 更新计数（delta = -1 表示减一条）
        musicMapper.updateCommentCount(musicId, -1);
    }
}
```

### 14.4 LikeService

**文件路径**：`music-platform/src/main/java/com/gm/music/service/LikeService.java`

```java
package com.gm.music.service;

import com.gm.music.entity.LikeRecord;
import com.gm.music.mapper.LikeRecordMapper;
import com.gm.music.mapper.MusicMapper;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.HashMap;
import java.util.Map;

/**
 * 点赞服务
 * Toggle 模式：
 *   如果已赞 → 删除记录，like_count - 1
 *   如果未赞 → 插入记录，like_count + 1
 */
@Service
public class LikeService {

    private final LikeRecordMapper likeRecordMapper;
    private final MusicMapper musicMapper;

    public LikeService(LikeRecordMapper likeRecordMapper, MusicMapper musicMapper) {
        this.likeRecordMapper = likeRecordMapper;
        this.musicMapper = musicMapper;
    }

    /**
     * 切换点赞状态
     * @return {liked: true/false} 操作后的状态
     */
    @Transactional
    public Map<String, Object> toggle(Integer userId, Integer musicId) {
        LikeRecord existing = likeRecordMapper.findByUserAndMusic(userId, musicId);

        Map<String, Object> result = new HashMap<>();

        if (existing != null) {
            // 已赞 → 取消点赞
            likeRecordMapper.delete(userId, musicId);
            musicMapper.updateLikeCount(musicId, -1);
            result.put("liked", false);
        } else {
            // 未赞 → 点赞
            LikeRecord record = new LikeRecord();
            record.setUserId(userId);
            record.setMusicId(musicId);
            likeRecordMapper.insert(record);
            musicMapper.updateLikeCount(musicId, 1);
            result.put("liked", true);
        }

        return result;
    }

    /** 查询当前用户是否已赞 */
    public boolean isLiked(Integer userId, Integer musicId) {
        return likeRecordMapper.findByUserAndMusic(userId, musicId) != null;
    }
}
```

### 14.5 RankingService

**文件路径**：`music-platform/src/main/java/com/gm/music/service/RankingService.java`

注：这里直接用 MyBatis XML 的复杂 SQL 实现排行查询，不需要额外的 Mapper 方法。

```java
package com.gm.music.service;

import com.gm.music.entity.Music;
import org.springframework.stereotype.Service;

import javax.sql.DataSource;
import java.sql.*;
import java.time.DayOfWeek;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.LocalTime;
import java.util.ArrayList;
import java.util.List;

/**
 * 排行榜服务
 * 使用 JDBC 原生查询执行复杂聚合 SQL
 * 统计本周（周一 00:00 到周日 23:59）的各项数据
 */
@Service
public class RankingService {

    private final DataSource dataSource;

    public RankingService(DataSource dataSource) {
        this.dataSource = dataSource;
    }

    /**
     * 获取本周的起止时间
     * 周一 00:00:00 到 周日 23:59:59
     */
    private String[] getWeekRange() {
        LocalDate today = LocalDate.now();
        // 找到本周一
        LocalDate monday = today.with(DayOfWeek.MONDAY);
        // 找到本周日
        LocalDate sunday = monday.plusDays(6);

        String start = monday.atStartOfDay().toString().replace("T", " ");  // "2024-01-08 00:00:00"
        String end = sunday.atTime(LocalTime.MAX).toString().replace("T", " ");  // "2024-01-14 23:59:59"
        return new String[]{start, end};
    }

    /** 本周点赞 Top10（基于 like_record 表统计） */
    public List<Music> getWeeklyLikeTop10() {
        String[] range = getWeekRange();
        String sql = """
            SELECT m.*, u.username, COUNT(lr.id) AS weekly_count
            FROM like_record lr
            JOIN music m ON lr.music_id = m.id
            JOIN user u ON m.user_id = u.id
            WHERE lr.create_time BETWEEN ? AND ?
            GROUP BY lr.music_id
            ORDER BY weekly_count DESC
            LIMIT 10
            """;
        return queryRanking(sql, range[0], range[1]);
    }

    /** 本周下载 Top10 */
    public List<Music> getWeeklyDownloadTop10() {
        String[] range = getWeekRange();
        String sql = """
            SELECT m.*, u.username, COUNT(dr.id) AS weekly_count
            FROM download_record dr
            JOIN music m ON dr.music_id = m.id
            JOIN user u ON m.user_id = u.id
            WHERE dr.create_time BETWEEN ? AND ?
            GROUP BY dr.music_id
            ORDER BY weekly_count DESC
            LIMIT 10
            """;
        return queryRanking(sql, range[0], range[1]);
    }

    /** 本周评论 Top10 */
    public List<Music> getWeeklyCommentTop10() {
        String[] range = getWeekRange();
        String sql = """
            SELECT m.*, u.username, COUNT(c.id) AS weekly_count
            FROM comment c
            JOIN music m ON c.music_id = m.id
            JOIN user u ON m.user_id = u.id
            WHERE c.create_time BETWEEN ? AND ?
            GROUP BY c.music_id
            ORDER BY weekly_count DESC
            LIMIT 10
            """;
        return queryRanking(sql, range[0], range[1]);
    }

    /** 执行排行 SQL 并返回 Music 列表 */
    private List<Music> queryRanking(String sql, String start, String end) {
        List<Music> list = new ArrayList<>();
        try (Connection conn = dataSource.getConnection();
             PreparedStatement ps = conn.prepareStatement(sql)) {
            ps.setString(1, start);
            ps.setString(2, end);
            try (ResultSet rs = ps.executeQuery()) {
                while (rs.next()) {
                    Music music = new Music();
                    music.setId(rs.getInt("id"));
                    music.setTitle(rs.getString("title"));
                    music.setArtist(rs.getString("artist"));
                    music.setDescription(rs.getString("description"));
                    music.setFilePath(rs.getString("file_path"));
                    music.setFileSize(rs.getLong("file_size"));
                    music.setLikeCount(rs.getInt("like_count"));
                    music.setCommentCount(rs.getInt("comment_count"));
                    music.setDownloadCount(rs.getInt("download_count"));
                    music.setUserId(rs.getInt("user_id"));
                    music.setUsername(rs.getString("username"));
                    list.add(music);
                }
            }
        } catch (SQLException e) {
            throw new RuntimeException("排行查询失败", e);
        }
        return list;
    }
}
```

**知识点**：
- 排行榜的 SQL 是标准的分组聚合：`GROUP BY music_id → COUNT → ORDER BY DESC → LIMIT 10`
- `BETWEEN ? AND ?` 查本周时间范围内的记录
- 这里用 JDBC 原生查询而不是 MyBatis XML，是为了教学目的展示两种方式
- `LocalDate.with(DayOfWeek.MONDAY)` 找到当周的周一

---

## 第十五步：Controller 层（处理 HTTP 请求）

### 15.1 AuthController（登录/注册）

**文件路径**：`music-platform/src/main/java/com/gm/music/controller/AuthController.java`

```java
package com.gm.music.controller;

import com.gm.music.common.Result;
import com.gm.music.config.JwtUtils;
import com.gm.music.entity.User;
import com.gm.music.service.UserService;
import org.springframework.web.bind.annotation.*;

import java.util.Map;

/**
 * 认证控制器
 * @RestController = @Controller + @ResponseBody（所有方法返回 JSON）
 * @RequestMapping("/api/auth") 这个类的所有请求路径都以 /api/auth 开头
 */
@RestController
@RequestMapping("/api/auth")
public class AuthController {

    private final UserService userService;
    private final JwtUtils jwtUtils;

    public AuthController(UserService userService, JwtUtils jwtUtils) {
        this.userService = userService;
        this.jwtUtils = jwtUtils;
    }

    /**
     * 登录
     * POST /api/auth/login
     * 请求体 JSON：{"username": "admin", "password": "123"}
     * @RequestBody 自动把 JSON 转成 Java Map
     */
    @PostMapping("/login")
    public Result<?> login(@RequestBody Map<String, String> loginData) {
        String username = loginData.get("username");
        String password = loginData.get("password");

        // 1. 查用户
        User user = userService.findByUsername(username);
        if (user == null) {
            return Result.fail("用户名或密码错误");
        }

        // 2. 检查是否被禁用
        if (user.getEnabled() != null && !user.getEnabled()) {
            return Result.fail("账号已被禁用");
        }

        // 3. 验证密码
        if (!userService.validatePassword(password, user.getPassword())) {
            return Result.fail("用户名或密码错误");
        }

        // 4. 生成 JWT
        String token = jwtUtils.generateToken(user.getId(), user.getUsername(), user.getRole());

        // 5. 返回 token + 用户信息
        return Result.success(Map.of(
                "token", token,
                "username", user.getUsername(),
                "role", user.getRole()
        ));
    }

    /**
     * 注册
     * POST /api/auth/register
     */
    @PostMapping("/register")
    public Result<?> register(@RequestBody Map<String, String> data) {
        try {
            User user = userService.register(
                    data.get("username"),
                    data.get("password"),
                    data.get("name"),
                    data.get("email")
            );
            return Result.success(Map.of("id", user.getId(), "username", user.getUsername()));
        } catch (RuntimeException e) {
            return Result.fail(e.getMessage());
        }
    }
}
```

**知识点**：
- `@RestController`：这个类的所有方法返回的数据会自动转为 JSON
- `@PostMapping("/login")`：处理 POST 请求，完整 URL 是 `POST /api/auth/login`
- `@RequestBody`：Spring 自动把请求体的 JSON 转成 Java 对象
- `Map.of("token", token, ...)`：Java 9+ 的便捷创建 Map 的方式

### 15.2 MusicController

**文件路径**：`music-platform/src/main/java/com/gm/music/controller/MusicController.java`

```java
package com.gm.music.controller;

import com.gm.music.common.Result;
import com.gm.music.common.ResultCode;
import com.gm.music.entity.Comment;
import com.gm.music.entity.Music;
import com.gm.music.service.CommentService;
import com.gm.music.service.MusicService;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.security.core.Authentication;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;

import java.io.*;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.List;
import java.util.Map;

@RestController
@RequestMapping("/api/music")
public class MusicController {

    private final MusicService musicService;
    private final CommentService commentService;

    @Value("${music.file-path}")
    private String filePath;

    public MusicController(MusicService musicService, CommentService commentService) {
        this.musicService = musicService;
        this.commentService = commentService;
    }

    /**
     * 分页搜索音乐列表
     * GET /api/music/list?page=1&size=10&keyword=周杰伦
     */
    @GetMapping("/list")
    public Result<?> list(@RequestParam(defaultValue = "1") int page,
                          @RequestParam(defaultValue = "10") int size,
                          @RequestParam(required = false) String keyword) {
        List<Music> list = musicService.list(page, size, keyword);
        int total = musicService.count(keyword);
        return Result.success(Map.of("list", list, "total", total, "page", page, "size", size));
    }

    /**
     * 音乐详情 + 评论列表
     * GET /api/music/{id}
     * @PathVariable 从 URL 路径中取值，{id} 是路径变量
     */
    @GetMapping("/{id}")
    public Result<?> detail(@PathVariable Integer id) {
        Music music = musicService.findById(id);
        if (music == null) {
            return Result.fail(ResultCode.NOT_FOUND);
        }
        List<Comment> comments = commentService.listByMusicId(id);
        return Result.success(Map.of("music", music, "comments", comments));
    }

    /**
     * 上传音乐（需要登录）
     * POST /api/music/upload
     * 表单格式上传（multipart/form-data），不是 JSON
     */
    @PostMapping("/upload")
    public Result<?> upload(@RequestParam("file") MultipartFile file,
                            @RequestParam String title,
                            @RequestParam String artist,
                            @RequestParam(required = false) String description) {
        try {
            Integer userId = getCurrentUserId();  // 从 JWT 中获取当前用户 ID
            Music music = musicService.upload(file, title, artist, description, userId);
            return Result.success(music);
        } catch (RuntimeException e) {
            return Result.fail(e.getMessage());
        } catch (IOException e) {
            return Result.fail("文件上传失败");
        }
    }

    /**
     * 下载音乐
     * GET /api/music/{id}/download
     * 直接流式输出 MP3 文件到浏览器
     */
    @GetMapping("/{id}/download")
    public void download(@PathVariable Integer id, HttpServletResponse response) {
        Music music = musicService.findById(id);
        if (music == null) {
            response.setStatus(404);
            return;
        }

        // 记录下载（无论是否登录都记录，如果有登录信息则带上 userId）
        Integer userId = getCurrentUserIdOrNull();
        if (userId != null) {
            musicService.download(id, userId);
        }

        // 读取文件并写入响应流
        File file = new File(filePath + music.getFilePath());
        if (!file.exists()) {
            response.setStatus(404);
            return;
        }

        try {
            // 设置响应头：告诉浏览器这是一个下载文件
            response.setContentType("audio/mpeg");
            response.setHeader("Content-Disposition",
                    "attachment; filename=" + URLEncoder.encode(music.getTitle() + ".mp3", StandardCharsets.UTF_8));
            response.setContentLengthLong(file.length());

            // 流式输出（边读边写，内存占用小）
            try (FileInputStream fis = new FileInputStream(file);
                 OutputStream os = response.getOutputStream()) {
                byte[] buffer = new byte[8192];
                int bytesRead;
                while ((bytesRead = fis.read(buffer)) != -1) {
                    os.write(buffer, 0, bytesRead);
                }
                os.flush();
            }
        } catch (IOException e) {
            response.setStatus(500);
        }
    }

    /**
     * 删除音乐（需要是上传者或管理员）
     */
    @DeleteMapping("/{id}")
    public Result<?> delete(@PathVariable Integer id) {
        Music music = musicService.findById(id);
        if (music == null) {
            return Result.fail(ResultCode.NOT_FOUND);
        }

        Integer userId = getCurrentUserId();
        String role = getCurrentUserRole();

        // 检查权限：只有上传者本人或管理员可以删除
        if (!userId.equals(music.getUserId()) && !"admin".equals(role)) {
            return Result.fail(ResultCode.FORBIDDEN);
        }

        musicService.delete(id);
        return Result.success();
    }

    // ========== 工具方法 ==========

    /** 从 SecurityContext 获取当前登录用户 ID */
    private Integer getCurrentUserId() {
        Authentication auth = SecurityContextHolder.getContext().getAuthentication();
        return (Integer) auth.getPrincipal();  // 我们在 JwtFilter 中把 userId 设为了 Principal
    }

    /** 获取当前用户 ID（未登录时返回 null） */
    private Integer getCurrentUserIdOrNull() {
        try {
            return getCurrentUserId();
        } catch (Exception e) {
            return null;
        }
    }

    /** 获取当前用户角色 */
    private String getCurrentUserRole() {
        Authentication auth = SecurityContextHolder.getContext().getAuthentication();
        return auth.getAuthorities().iterator().next().getAuthority()
                .replace("ROLE_", "").toLowerCase();
    }
}
```

**知识点**：
- `@RequestParam(defaultValue = "1")`：参数可以不传，默认值是 "1"
- `@PathVariable`：从 URL 路径 `/api/music/123` 中提取 `123`
- 文件下载用的是"流式输出"：一边从硬盘读取，一边写入响应流，不会把整个文件加载到内存中
- `byte[] buffer = new byte[8192]`：8KB 的缓冲区，边读边写
